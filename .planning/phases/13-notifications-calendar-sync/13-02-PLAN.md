---
phase: 13-notifications-calendar-sync
plan: 02
type: execute
wave: 2
depends_on: ["13-01-PLAN.md"]
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/push/push.service.ts
  - backend/src/notifications/notification-delivery.service.ts
  - backend/src/notifications/notification-delivery.module.ts
  - backend/src/notifications/notification-types.ts
  - backend/src/bookings/bookings.service.ts
  - backend/src/main.ts
autonomous: true
requirements: [R9]
user_setup:
  - "Backend env: confirm `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` configured."
  - "Backend env: configure scheduler cadence via `REMINDER_SCAN_CRON` (optional)."
must_haves:
  truths:
    - "Reminder job sends at-most-once per booking+type+scheduledFor (idempotent)."
    - "Fighter new-booking push sends once per booking creation (idempotent)."
    - "Invalid push subscriptions are cleaned up on 404/410 responses."
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: "NotificationDelivery idempotency table with unique key"
    - path: backend/src/notifications/notification-delivery.service.ts
      provides: "Cron scan + idempotent send runner"
---

<objective>
Implement actual push delivery and reminder scheduling with strong idempotency: send `REMINDER_24H` to booking customer and `FIGHTER_NEW_BOOKING` to fighter, never twice per unique key, and cleanup dead subscriptions.
</objective>

<context>
@.planning/phases/13-notifications-calendar-sync/13-CONTEXT.md
@.planning/phases/13-notifications-calendar-sync/13-RESEARCH.md
@backend/src/bookings/bookings.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NotificationDelivery table for idempotency + audit</name>
  <files>backend/prisma/schema.prisma</files>
  <action>
Add `NotificationDelivery` model:

- `id` cuid
- `userId` (recipient) FK User
- `bookingId` FK Booking (required for Phase 13 notification types)
- `type` enum/string: `REMINDER_24H | FIGHTER_NEW_BOOKING`
- `scheduledFor` DateTime (when should be sent; for immediate sends use createdAt)
- `status` enum/string: `pending|sent|failed`
- `attemptCount` int default 0
- `lastError` string? (truncated)
- `sentAt` DateTime?
- `createdAt`, `updatedAt`

Unique constraint (idempotency key):
- `@@unique([userId, bookingId, type, scheduledFor])`

Index:
- `@@index([status, scheduledFor])`

Run migration.
  </action>
  <acceptance_criteria>
    - Can insert `pending` record; duplicate insert on same key fails.
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
  </verify>
  <done>
    DB can enforce at-most-once send semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add push delivery implementation (VAPID) + cleanup invalid subs</name>
  <files>backend/src/push/push.service.ts,backend/src/notifications/notification-types.ts</files>
  <action>
Add dependency (choose one; prefer `node-webpush` per research):
- `node-webpush` (or `web-push` if already present)

In `PushService`, implement `sendToUser(userId, payload)`:
- load subscriptions for user
- for each subscription, attempt send with VAPID
- on HTTP 404/410 from push service: delete subscription (endpoint no longer valid)
- keep payload small; avoid PII

Payload contract (JSON, sent as web push payload):
- `type`: `REMINDER_24H | FIGHTER_NEW_BOOKING`
- `bookingId`: string
- `startAt`: ISO string
- `url`: string (in-app path; ok if missing — worker can fall back to `/`)

Payload must be PII-free by construction:
- do NOT include names, phone, email, address in push payload
- service worker renders generic copy based on `type`

Define notification types constants:
- `REMINDER_24H`
- `FIGHTER_NEW_BOOKING`
  </action>
  <acceptance_criteria>
    - Service compiles and can attempt send to a known subscription.
    - Dead endpoints removed after send attempt.
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
    <manual>Send test push to local subscribed browser (dev)</manual>
  </verify>
  <done>
    Backend can send web push and maintains subscription hygiene.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement idempotent immediate send for fighter new booking</name>
  <files>backend/src/notifications/notification-delivery.service.ts,backend/src/bookings/bookings.service.ts</files>
  <action>
Create `NotificationDeliveryService.sendFighterNewBooking(booking)`:
- determine fighter userId recipient
- check fighter preference `fighterNewBookingEnabled`
- compute `scheduledFor = booking.createdAt` (or `now`)
- try create `NotificationDelivery(pending)` with unique key
  - if unique violation: already handled → return
- call `PushService.sendToUser(...)`
- update row: status sent/failed, attemptCount++, sentAt, lastError on failure

Wire booking creation hook to call this method (replace stub from 13-01).
  </action>
  <acceptance_criteria>
    - Creating same booking twice (or retry path) does not duplicate push sends (unique key blocks).
    - Preference OFF prevents send.
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
  </verify>
  <done>
    Fighter new booking notification is delivered at-most-once.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement reminder scan job (24h before) with idempotency</name>
  <files>backend/src/notifications/notification-delivery.module.ts,backend/src/notifications/notification-delivery.service.ts,backend/src/main.ts</files>
  <action>
Add scheduling (prefer `@nestjs/schedule`):
- add dependency `@nestjs/schedule`
- enable scheduler in app module if not already

Cron logic (scan window):
- every X minutes, compute target window for `REMINDER_24H`:
  - `start = now + 24h`
  - `end = now + 24h + scanInterval` (or +/- tolerance)
- query bookings starting within [start, end] and in `confirmed` state (match existing booking status model in `backend/src/bookings/**`)
- for each booking:
  - recipient = booking.customer userId
  - check preference `remindersEnabled`
  - scheduledFor = booking.startAt - 24h (rounded to minute)
  - create `NotificationDelivery(pending)` with unique key
    - if exists: skip (already scheduled/sent)
  - send via `PushService.sendToUser` with payload type `REMINDER_24H` and user-facing `title/body`
  - update delivery row

Time math:
- Use UTC instants from DB.
- Do not try to localize timezone until Phase 11; include `startAt` ISO and let client render.
  </action>
  <acceptance_criteria>
    - Job can run repeatedly without duplicates (unique constraint).
    - Preference OFF prevents reminder.
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
    <manual>Create booking starting ~24h+few minutes; wait for scan; observe delivery row and push.</manual>
  </verify>
  <done>
    Reminder delivery exists and is at-most-once per unique key.
  </done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| Duplicate sends on retries/restarts | Reliability | Unique key on `(userId, bookingId, type, scheduledFor)` + create-before-send workflow; update status after send. |
| Abuse: attacker creates many bookings to trigger spam | Abuse | Respect preferences; later add per-user send limits; keep payload minimal; admin monitoring via delivery table. |
| Push provider errors leak sensitive info | Info disclosure | Do not include PII in payload; only ids + times; fetch detail after user opens app. |
</threat_model>

<verification>
- Unit/integration tests for:
  - unique constraint prevents duplicate send attempt
  - preference disables
  - invalid subscription cleanup
- Manual dev smoke:
  - subscribe browser, create booking, observe fighter push
  - create booking near 24h mark, observe reminder push once
</verification>

