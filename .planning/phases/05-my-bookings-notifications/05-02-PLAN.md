---
phase: 05-my-bookings-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/app.routes.ts
  - src/app/core/services/booking.service.ts
  - src/app/pages/my-bookings/my-bookings.page.ts
  - src/app/pages/my-bookings/my-bookings.page.html
  - src/app/pages/booking-detail/booking-detail.page.ts
  - src/app/pages/booking-detail/booking-detail.page.html
autonomous: true
requirements:
  - MBB-01
  - MBB-02
user_setup: []

must_haves:
  truths:
    - "User can open My bookings and see Upcoming/Past tabs with correct default sorting"
    - "Each booking row shows fighter name, service title, Pacific local date/time, and status chip"
    - "User can open booking detail and see fighter/service/time/status/payment state/price"
    - "If booking awaiting payment, user sees Pay now CTA leading to existing payment flow"
  artifacts:
    - path: "src/app/pages/my-bookings/my-bookings.page.ts"
      provides: "List logic: fetch, tabs, loading/error/empty, navigation to detail"
    - path: "src/app/pages/my-bookings/my-bookings.page.html"
      provides: "List UI per UI-SPEC (skeleton, chips, CTA)"
    - path: "src/app/pages/booking-detail/booking-detail.page.ts"
      provides: "Detail fetch + Pay now CTA wiring"
    - path: "src/app/pages/booking-detail/booking-detail.page.html"
      provides: "Detail UI per UI-SPEC"
    - path: "src/app/core/services/booking.service.ts"
      provides: "listMyBookings() + updated Booking model fields"
    - path: "src/app/app.routes.ts"
      provides: "Auth-guarded routes for my bookings and booking detail"
  key_links:
    - from: "src/app/pages/my-bookings/my-bookings.page.ts"
      to: "src/app/core/services/booking.service.ts"
      via: "booking.listMyBookings() withCredentials"
      pattern: "listMyBookings\\("
    - from: "src/app/app.routes.ts"
      to: "src/app/pages/my-bookings/my-bookings.page.ts"
      via: "loadComponent route + authGuard"
      pattern: "path:\\s*'my-bookings'"
    - from: "src/app/pages/booking-detail/booking-detail.page.ts"
      to: "existing pay route"
      via: "navigate to /pay/return or payment start CTA"
      pattern: "pay"
---

<objective>
Implement Ionic/Angular “My bookings” list (Upcoming/Past tabs) and booking detail page, matching Phase 05 UI-SPEC and decisions D-01..D-08, and wiring to backend APIs from Plan 05-01.

Purpose: Users self-serve booking status (upcoming/past/expired/confirmed) and can continue payment for awaiting-payment bookings.
Output: New pages + routes + BookingService list method + templates per UI-SPEC.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-my-bookings-notifications/05-CONTEXT.md
@.planning/phases/05-my-bookings-notifications/05-UI-SPEC.md
@.planning/phases/05-my-bookings-notifications/05-PATTERNS.md

Locked UI decisions to implement exactly:
- D-01: 2 tabs (Upcoming/Past)
- D-02: row shows fighter name, service title, Pacific local date/time, status chip
- D-03: default sorting (Upcoming soonest-first; Past newest-first)
- D-04: empty state w/ message + CTA Explore fighters
- D-05: detail fields fighter/service/local time/status/payment state/price
- D-06: show Pacific time only, no timezone label
- D-07: awaiting_payment shows Pay now CTA (no cancel)
- D-08: terminal statuses show no actions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth-guarded routes and BookingService list method</name>
  <files>src/app/app.routes.ts, src/app/core/services/booking.service.ts</files>
  <read_first>
    - src/app/app.routes.ts (lazy route + authGuard pattern)
    - src/app/core/services/booking.service.ts (withCredentials + existing getBooking)
    - src/app/pages/booking-success/booking-success.page.ts (booking fetch on enter pattern)
  </read_first>
  <acceptance_criteria>
    - Route `/my-bookings` exists and is guarded by `authGuard`.
    - Route `/bookings/:bookingId` (deep link friendly) exists and is guarded by `authGuard`.
    - `BookingService` has `listMyBookings()` calling `GET {baseUrl}/bookings` with `withCredentials: true`.
    - Client-side types include fields needed by UI-SPEC (fighterName, serviceTitle, startsAtUtc, status, price/payment fields).
  </acceptance_criteria>
  <action>
    In `src/app/app.routes.ts`:
    - Add route `my-bookings` loading `MyBookingsPage`, guarded by `authGuard`.
    - Add route `bookings/:bookingId` loading `BookingDetailPage`, guarded by `authGuard`.

    In `src/app/core/services/booking.service.ts`:
    - Extend `Booking` model (or create a list item type) to include display fields returned by backend list/detail DTOs.
    - Add `listMyBookings(): Observable<Booking[] /* or BookingListItem[] */>` that calls `/bookings` with `{ withCredentials: true }`.
    - Keep API baseUrl and error-handling style consistent with existing methods (no `any` in templates).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Routes compile, BookingService exposes `listMyBookings()` and app builds/tests run.</done>
</task>

<task type="auto">
  <name>Task 2: Implement “My bookings” list page with Upcoming/Past tabs + states</name>
  <files>src/app/pages/my-bookings/my-bookings.page.ts, src/app/pages/my-bookings/my-bookings.page.html</files>
  <read_first>
    - src/app/pages/catalog/catalog.page.ts + .html (loading/skeleton/error patterns)
    - src/app/pages/payment-return/payment-return.page.ts (copy/CTA conventions)
    - DESIGN.md (no-line rule; tonal layering) (per D-17)
  </read_first>
  <acceptance_criteria>
    - Two tabs exist (Upcoming/Past) implemented with Ionic primitives (segment/tabs) and match UI-SPEC.
    - Upcoming sorts soonest-first by booking start time; Past sorts newest-first.
    - Row shows fighterName, serviceTitle, Pacific local date/time (no timezone label), and status chip.
    - Empty state matches copy contract + CTA Explore fighters routes to existing catalog (`/explore`).
    - Error state shows inline card with retry, uses gradient button style where applicable.
    - Loading state uses skeleton rows (not spinner-only).
  </acceptance_criteria>
  <action>
    Create `MyBookingsPage` as standalone Ionic page mirroring `CatalogPage` patterns:
    - Use `ionViewWillEnter()` to fetch.
    - Maintain `loading`, `error`, `bookings` state.
    - Derive `upcomingBookings` and `pastBookings` arrays from `bookings` and selected tab.
      - Classify by status + startsAtUtc:
        - Terminal `expired` should appear under Past (per research recommendation to avoid "upcoming but dead").
        - Confirmed bookings with startsAtUtc in past appear in Past; in future appear Upcoming.
    - Sorting:
      - Upcoming: startsAtUtc asc
      - Past: startsAtUtc desc
    - Format time in Pacific without label (D-06): use Luxon on client only if already present; otherwise use `Intl.DateTimeFormat` with `timeZone: 'America/Los_Angeles'` and omit timezone name.

    Template requirements:
    - Use tonal surfaces (no 1px dividers); chip uses tonal background.
    - Row is tappable, navigates to `/bookings/:bookingId`.
    - Empty state includes Explore fighters CTA -> `/explore`.
    - Pull-to-refresh optional (UI-SPEC says recommended); implement with `IonRefresher` if easy without introducing new patterns.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>My bookings renders with skeleton/error/empty; upcoming/past split + sort correct; navigation to detail route works.</done>
</task>

<task type="auto">
  <name>Task 3: Implement booking detail page with Pay now CTA for awaiting payment</name>
  <files>src/app/pages/booking-detail/booking-detail.page.ts, src/app/pages/booking-detail/booking-detail.page.html</files>
  <read_first>
    - src/app/pages/booking-success/booking-success.page.ts + .html (booking fetch + states)
    - src/app/core/services/booking.service.ts (getBooking)
  </read_first>
  <acceptance_criteria>
    - Detail route reads `bookingId` path param and fetches booking on `ionViewWillEnter()`.
    - Shows fighter, service, Pacific local time (no label), status, payment state, price.
    - If `status === 'awaiting_payment'`, shows Pay now primary CTA using accent gradient; no cancel action.
    - For terminal statuses (confirmed/expired/past), shows no CTA.
    - CTA wires to existing payment flow (reuse pattern from payment-return/success flow): navigate user to the existing "pay now" initiation route used in Phase 4.
  </acceptance_criteria>
  <action>
    Create standalone `BookingDetailPage` with same loading/error/data pattern as `BookingSuccessPage`, but uses `route.paramMap.get('bookingId')`.
    Render fields in tonal card stack per UI-SPEC.
    Implement Pay now CTA:
    - Prefer reusing existing route used by “Pay now” in prior flows (search existing pages for navigation target).
    - Pass bookingId in query params if required by payment-return flow.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Booking detail matches UI-SPEC decisions D-05..D-08; awaiting_payment shows Pay now CTA and no other actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router/URL → Client | Untrusted input in `:bookingId` param |
| Client → API | Cookies sent; must rely on server scoping (handled in Plan 05-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T (Tampering) | `bookingId` route param | mitigate | Validate presence; encode when calling API; rely on server 404 on unauthorized ID. Show friendly error state. |
| T-05-05 | I (Info disclosure) | UI renders booking fields | accept | UI only renders API response; server already scopes by user. Avoid logging sensitive fields in client. |
</threat_model>

<verification>
- Frontend unit tests/build: `npm test`
- Manual smoke (optional): navigate `/my-bookings` → tap row → see detail → if awaiting_payment, tap Pay now
</verification>

<success_criteria>
- “My bookings” exists with Upcoming/Past tabs, correct sorting, scan-friendly rows, and Explore fighters empty state CTA
- Booking detail exists and shows required fields; Pay now CTA appears only for awaiting_payment
- Time shown in Pacific local time without timezone label
</success_criteria>

<output>
After completion, create `.planning/phases/05-my-bookings-notifications/05-02-SUMMARY.md`
</output>

