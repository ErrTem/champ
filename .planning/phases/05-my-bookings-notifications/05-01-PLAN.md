---
phase: 05-my-bookings-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/bookings/bookings.controller.ts
  - backend/src/bookings/bookings.service.ts
  - backend/src/bookings/dto/booking.dto.ts
  - backend/test/bookings.my-bookings.e2e-spec.ts
autonomous: true
requirements:
  - MBB-01
  - MBB-02
user_setup: []

must_haves:
  truths:
    - "Signed-in user can list only their bookings, split into Upcoming and Past with correct default sorting"
    - "Signed-in user can open booking detail showing fighter/service/time/status/payment state/price fields"
    - "Bookings with expired payment holds are returned as terminal status (expired) to avoid 'awaiting_payment forever'"
  artifacts:
    - path: "backend/src/bookings/bookings.controller.ts"
      provides: "GET /bookings (auth-scoped) and existing GET /bookings/:id remains user-scoped"
    - path: "backend/src/bookings/bookings.service.ts"
      provides: "User-scoped list query + enriched joins + expiry transition helper"
    - path: "backend/src/bookings/dto/booking.dto.ts"
      provides: "DTOs include list+detail display fields (fighter/service/time/status/payment/price)"
    - path: "backend/test/bookings.my-bookings.e2e-spec.ts"
      provides: "Nyquist automated coverage for list+detail scoping/sorting and expiry classification"
  key_links:
    - from: "backend/src/bookings/bookings.controller.ts"
      to: "backend/src/bookings/bookings.service.ts"
      via: "GET /bookings calls service with req.user.sub"
      pattern: "getMyBookings\\("
    - from: "backend/src/bookings/bookings.service.ts"
      to: "Prisma booking/slot/service/fighter tables"
      via: "findMany/select with joins"
      pattern: "prisma\\.booking\\.findMany"
    - from: "backend/src/bookings/bookings.service.ts"
      to: "booking.status"
      via: "conditional update to 'expired' when expiresAtUtc < now"
      pattern: "status:\\s*'expired'"
---

<objective>
Expose user-scoped “My bookings” backend API (list + enriched detail DTO), and ensure awaiting-payment holds become a stable terminal `expired` status once `expiresAtUtc` passes.

Purpose: Enable Phase 05 UI to render Upcoming/Past list + detail without leaking other users’ bookings (ASVS V4), and prevent stale `awaiting_payment` bookings from lingering indefinitely.
Output: `GET /bookings` endpoint + enriched booking DTO + e2e tests + expiry classification helper.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-my-bookings-notifications/05-CONTEXT.md
@.planning/phases/05-my-bookings-notifications/05-RESEARCH.md
@.planning/phases/05-my-bookings-notifications/05-PATTERNS.md

Key locked decisions to satisfy:
- D-01..D-04: UI requires Upcoming/Past split, scan-friendly fields, sort order, empty state CTA (API must provide enough data)
- D-18: auth is cookie-based; endpoints must require `JwtAccessAuthGuard` and infer userId from `req.user.sub` only (no userId params)
- D-19: UTC storage, Pacific display — API should return UTC ISO strings; client will format

Existing patterns:
- Controller guard + scoping: `backend/src/bookings/bookings.controller.ts` + `JwtAccessAuthGuard` (see 05-PATTERNS)
- DTO style: `backend/src/bookings/dto/booking.dto.ts` is plain TS shapes with ISO strings
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Nyquist Wave 0): Add backend e2e tests for “My bookings” list+detail</name>
  <files>backend/test/bookings.my-bookings.e2e-spec.ts</files>
  <read_first>
    - backend/test/*.e2e-spec.ts (pick closest auth-required pattern)
    - backend/src/bookings/bookings.controller.ts
    - backend/src/bookings/bookings.service.ts
  </read_first>
  <acceptance_criteria>
    - Tests cover MBB-01: `GET /bookings` returns only authenticated user bookings (no IDOR), and response supports Upcoming/Past split + sort expectations.
    - Tests cover MBB-02: `GET /bookings/:id` forbids access to other user booking (404) and returns enriched fields needed for detail view.
    - Tests cover expiry classification: if booking `status=awaiting_payment` and `expiresAtUtc` is in past, list/detail returns `status='expired'`.
  </acceptance_criteria>
  <action>
    Create new e2e spec file that:
    - Logs in (reuse existing auth test helpers/patterns) and calls `GET /bookings` with auth cookie.
    - Seeds two users and multiple bookings (upcoming, past) using Prisma test setup already used in other e2e tests.
    - Asserts only user A bookings returned for user A session.
    - Asserts default ordering: upcoming sorted by soonest `slot.startsAtUtc`, past sorted by newest.
    - Adds one expired-hold booking (awaiting_payment, expiresAtUtc in past) and asserts API returns `status='expired'` (even if stored as awaiting_payment before sweep).
  </action>
  <verify>
    <automated>cd backend; npm test -- bookings.my-bookings.e2e-spec.ts</automated>
  </verify>
  <done>Test file runs and fails for missing endpoint/fields (expected before implementation).</done>
</task>

<task type="auto">
  <name>Task 1: Implement `GET /bookings` (auth-scoped) returning enriched list DTO</name>
  <files>backend/src/bookings/bookings.controller.ts, backend/src/bookings/bookings.service.ts, backend/src/bookings/dto/booking.dto.ts</files>
  <read_first>
    - backend/src/bookings/bookings.controller.ts (existing get-by-id pattern)
    - backend/src/bookings/bookings.service.ts (Prisma select shaping + DTO return)
    - backend/src/bookings/dto/booking.dto.ts (baseline DTO types)
  </read_first>
  <acceptance_criteria>
    - `GET /bookings` exists, guarded by `JwtAccessAuthGuard`, scopes by `req.user.sub` only.
    - Response provides fields required by UI decisions D-02 and detail D-05: fighter name, service title, UTC starts/ends, booking status, expiresAtUtc, payment state (derive), price (from service price), plus booking id.
    - Endpoint supports Upcoming/Past split without client guessing: include `startsAtUtc` and `status` so client can categorize; server returns single list with stable fields.
  </acceptance_criteria>
  <action>
    Add `@Get()` handler in `BookingsController` using `@UseGuards(JwtAccessAuthGuard)` and `req.user.sub`.
    Implement `BookingsService.getMyBookings({ userId })`:
    - Query Prisma `booking.findMany({ where: { userId }, select: ... })` selecting:
      - booking: `id`, `status`, `expiresAtUtc`, `stripeCheckoutSessionId?` (for payment-state derivation if needed)
      - slot: `startsAtUtc`, `endsAtUtc`
      - fighter: `id`, `name`
      - service: `id`, `title`, `priceCents` (or actual price field in schema)
    - Return DTO array with ISO strings and display fields. Keep `status` as string; do NOT add new actions (out of scope).

    Extend DTOs in `booking.dto.ts`:
    - Create a list DTO shape used by list endpoint (can reuse/extend `BookingDto`, or add `BookingListItemDto`).
    - Include: `fighterName`, `serviceTitle`, `priceCents`, `paymentState` (string), `startsAtUtc`, `endsAtUtc`.

    Sorting: do NOT bake UI tab sorting into API response; tests may expect stable sort by `slot.startsAtUtc` asc/desc depending on filter; implement server-side optional sorting params only if tests require. Otherwise, keep response unsorted but deterministic by `slot.startsAtUtc` ascending, then `id`.
  </action>
  <verify>
    <automated>cd backend; npm test -- bookings.my-bookings.e2e-spec.ts</automated>
  </verify>
  <done>`GET /bookings` passes scoping and shape assertions in tests (except expiry sweep until Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotent expiry classification for stale `awaiting_payment` bookings</name>
  <files>backend/src/bookings/bookings.service.ts</files>
  <read_first>
    - backend/src/bookings/bookings.service.ts (transaction patterns)
    - backend/src/availability/availability.service.ts (nowUtc usage + lt filters) (per 05-PATTERNS)
  </read_first>
  <acceptance_criteria>
    - If a booking has `status='awaiting_payment'` and `expiresAtUtc < now`, API list and detail return `status='expired'`.
    - Expiry transition is idempotent (repeated calls do not flap or throw; status remains `expired`).
    - No notification emission here (NOT-02 handled in Plan 05-03); this plan only makes the state transition available/stable.
  </acceptance_criteria>
  <action>
    In `BookingsService` introduce helper `expireStaleAwaitingPaymentBookingsForUser(userId)` (or equivalent) called at start of `getMyBookings()` and `getBookingForUser()`:
    - Compute `nowUtc` once (`DateTime.utc().toJSDate()`).
    - Find bookings where `{ userId, status: 'awaiting_payment', expiresAtUtc: { lt: nowUtc } }`.
    - For each (or via updateMany), update status to `'expired'` only when still awaiting_payment (conditional update). Keep update idempotent.
    - Ensure it does not touch confirmed bookings.

    Keep DB writes minimal: prefer `updateMany` with filter rather than per-row updates unless you need IDs for later plans. If you return IDs, return them from helper for Plan 05-03 to emit notifications (but do NOT emit here).
  </action>
  <verify>
    <automated>cd backend; npm test -- bookings.my-bookings.e2e-spec.ts</automated>
  </verify>
  <done>Tests expecting expired classification pass; list+detail never return expired-hold bookings as `awaiting_payment` when `expiresAtUtc` already passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted user can call bookings endpoints with arbitrary IDs |
| API → DB | Access control enforced in query filters and joins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Info disclosure) | `GET /bookings`, `GET /bookings/:id` | mitigate | Enforce `JwtAccessAuthGuard` + scope Prisma `where: { userId: req.user.sub }` (no userId param). Add e2e tests asserting other-user booking is 404. |
| T-05-02 | T (Tampering) | expiry transition | mitigate | Conditional update only when `status='awaiting_payment'` and `expiresAtUtc < now`; idempotent repeated calls. |
| T-05-03 | D (DoS) | list endpoint triggers expiry sweep | accept | Sweep scoped to user only; bounded by user’s bookings count. If perf issue later, move to scheduler (future). |
</threat_model>

<verification>
- Backend tests green: `cd backend; npm test -- bookings.my-bookings.e2e-spec.ts`
</verification>

<success_criteria>
- `GET /bookings` exists, auth-scoped, returns enriched DTOs needed for UI (fighter/service/time/status/price/payment fields)
- Expired holds show as `expired` consistently in API responses
- No IDOR: user cannot list or fetch another user’s booking
</success_criteria>

<output>
After completion, create `.planning/phases/05-my-bookings-notifications/05-01-SUMMARY.md`
</output>

