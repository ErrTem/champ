---
phase: 05-my-bookings-notifications
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/src/notifications/notifications.module.ts
  - backend/src/notifications/notifications.service.ts
  - backend/src/payments/payments.service.ts
  - backend/src/bookings/bookings.service.ts
  - backend/test/notifications.booking-status.e2e-spec.ts
autonomous: true
requirements:
  - NOT-01
  - NOT-02
user_setup: []

must_haves:
  truths:
    - "When booking becomes confirmed (Stripe webhook path), backend emits one dev-email log with deep link to booking detail"
    - "When awaiting-payment hold expires and booking transitions to expired, backend emits one dev-email log with deep link"
    - "No emails sent on booking creation, or on Stripe cancel return"
  artifacts:
    - path: "backend/src/notifications/notifications.service.ts"
      provides: "Dev-email equivalent logger (minimal transactional payload + deep link)"
    - path: "backend/src/notifications/notifications.module.ts"
      provides: "Nest module wiring for NotificationsService"
    - path: "backend/src/payments/payments.service.ts"
      provides: "Triggers NOT-01 when confirm transition actually applied"
    - path: "backend/src/bookings/bookings.service.ts"
      provides: "Triggers NOT-02 when expiry transition actually applied (from Plan 05-01 helper)"
    - path: "backend/test/notifications.booking-status.e2e-spec.ts"
      provides: "Automated assertions for NOT-01/NOT-02 emission rules"
  key_links:
    - from: "backend/src/payments/payments.service.ts"
      to: "backend/src/notifications/notifications.service.ts"
      via: "emit after booking status changes awaiting_payment -> confirmed"
      pattern: "notifyBookingConfirmed"
    - from: "backend/src/bookings/bookings.service.ts"
      to: "backend/src/notifications/notifications.service.ts"
      via: "emit after booking status changes awaiting_payment -> expired"
      pattern: "notifyBookingExpired"
    - from: "backend/src/notifications/notifications.service.ts"
      to: "PUBLIC_APP_URL deep link"
      via: "new URL('/bookings/...', appUrl)"
      pattern: "PUBLIC_APP_URL"
---

<objective>
Implement Phase 05 notification delivery per decisions D-09..D-16: dev-only email equivalent (backend logs) triggered on booking confirmation and hold expiry, with deep link to booking detail.

Purpose: Satisfy NOT-01/NOT-02 without adding a real email provider (explicitly deferred), and keep emissions idempotent on retries / repeated sweeps.
Output: Notifications module/service + wiring from payments confirmation and booking expiry transitions + automated tests.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-my-bookings-notifications/05-CONTEXT.md
@.planning/phases/05-my-bookings-notifications/05-RESEARCH.md
@.planning/phases/05-my-bookings-notifications/05-PATTERNS.md

Locked decisions to implement exactly:
- D-09: dev-only email equivalent via backend logs (clear swap path; no provider SDK)
- D-10: sender `noreply@…`
- D-11: include deep link to booking detail in app
- D-12: minimal transactional tone
- D-13: send on booking confirmed (Stripe webhook path)
- D-14: treat hold expiry as cancellation-like event and notify
- D-15: do not send on booking creation
- D-16: do not send on Stripe cancel return

Existing precedents (see 05-PATTERNS):
- Dev-only link logging in `backend/src/auth/auth.service.ts`
- Deep link URL building from `PUBLIC_APP_URL` in `backend/src/payments/payments.service.ts`
- Stripe webhook idempotency + confirmed transition in `PaymentsService.processStripeEvent`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Nyquist Wave 0): Add backend tests asserting notification emission rules</name>
  <files>backend/test/notifications.booking-status.e2e-spec.ts</files>
  <read_first>
    - backend/test/payments.webhook.e2e-spec.ts (webhook harness + signature helper)
    - backend/src/payments/payments.service.ts (confirm transition code)
    - backend/src/bookings/bookings.service.ts (expiry helper from Plan 05-01)
  </read_first>
  <acceptance_criteria>
    - NOT-01: confirming a booking via Stripe webhook causes exactly one notification emit (logger call) containing deep link to booking detail.
    - NOT-02: expiring a stale awaiting-payment booking causes exactly one notification emit containing deep link.
    - Negative assertions: no emit on booking creation, no emit on Stripe cancel return, and retries/duplicates remain idempotent.
  </acceptance_criteria>
  <action>
    Create e2e test that:
    - Reuses Stripe webhook test helper to send the "checkout.session.completed" event for an awaiting_payment booking, then asserts notifications service/logger was called once.
    - Creates an expired-hold booking (awaiting_payment, expiresAtUtc in past) and triggers the expiry sweep path (call list endpoint or call service method via API path), then asserts one notification.
    - Uses Jest spies on Nest Logger (or inject NotificationsService and spy on a method) to avoid brittle log text matching.
  </action>
  <verify>
    <automated>cd backend; npm test -- notifications.booking-status.e2e-spec.ts</automated>
  </verify>
  <done>Test file runs and fails due to missing NotificationsService wiring/methods (expected before implementation).</done>
</task>

<task type="auto">
  <name>Task 1: Add Notifications module/service implementing dev-email equivalent logging + deep link</name>
  <files>backend/src/notifications/notifications.module.ts, backend/src/notifications/notifications.service.ts</files>
  <read_first>
    - backend/src/auth/auth.service.ts (dev-only Logger precedent)
    - backend/src/payments/payments.service.ts (`PUBLIC_APP_URL` URL construction)
  </read_first>
  <acceptance_criteria>
    - `NotificationsService` exposes explicit methods for Phase 05 events (confirmed, expired-hold) and logs minimal transactional payload.
    - Uses `process.env.PUBLIC_APP_URL` to build deep link to `/bookings/:bookingId` (D-11).
    - Does not include secrets in logs; logs only safe identifiers and human-facing fields (fighter/service/time if available).
    - Sender identity included in payload as `noreply@…` (D-10).
  </acceptance_criteria>
  <action>
    Create `backend/src/notifications/notifications.module.ts` exporting `NotificationsService`.
    Create `backend/src/notifications/notifications.service.ts`:
    - Use Nest `Logger`.
    - Implement methods:
      - `notifyBookingConfirmed(input: { bookingId: string; toEmail: string; fighterName?: string; serviceTitle?: string; startsAtUtc?: Date|string })`
      - `notifyBookingExpiredHold(input: { bookingId: string; toEmail: string; ... })`
    - For Phase 05 dev-only requirement (D-09): always log in development; in other envs, still log but clearly labeled as dev-equivalent unless a provider is later swapped in (keep swap path: centralize emission here).
    - Build deep link:
      - `const appUrl = process.env.PUBLIC_APP_URL; if (!appUrl) throw new Error('PUBLIC_APP_URL is required');`
      - `new URL(\`/bookings/${bookingId}\`, appUrl)`
    - Log message includes: event type (confirmed/expired), sender, recipient, bookingId, deepLink.
  </action>
  <verify>
    <automated>cd backend; npm test -- notifications.booking-status.e2e-spec.ts</automated>
  </verify>
  <done>Notifications module compiles and tests can spy on notification methods (still failing until wiring in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Wire notifications into confirmed + expiry transitions (idempotent)</name>
  <files>backend/src/payments/payments.service.ts, backend/src/bookings/bookings.service.ts</files>
  <read_first>
    - backend/src/payments/payments.service.ts (idempotency + confirmed transition)
    - backend/src/bookings/bookings.service.ts (expiry helper created in Plan 05-01)
  </read_first>
  <acceptance_criteria>
    - NOT-01: emit notification only when booking status transition awaiting_payment → confirmed actually applied (not on duplicate webhook events, and not if already confirmed).
    - NOT-02: emit notification only when booking status transition awaiting_payment → expired actually applied (not on repeated sweeps).
    - Ensure no emails on booking creation and Stripe cancel return (D-15, D-16).
  </acceptance_criteria>
  <action>
    Update `PaymentsService`:
    - Inject `NotificationsService`.
    - After confirmed transition succeeds, call `notifyBookingConfirmed(...)`.
    - Ensure call happens only when transition occurs. (Implementation hint: capture a boolean `didConfirm` inside transaction, or use conditional update that returns affected row count, then emit after transaction.)
    - Gather recipient email:
      - Join booking -> user email in Prisma select (or fetch user email separately) to pass `toEmail`. Keep within transaction only if needed; otherwise do a follow-up read after status confirmed.

    Update `BookingsService` expiry helper (from 05-01):
    - Inject `NotificationsService`.
    - Make helper return list of bookingIds transitioned (or include necessary fields), so you can emit only for those transitions.
    - Emit `notifyBookingExpiredHold(...)` once per transitioned booking.
    - Classify expiry as terminal; no actions required in UI (aligns D-08).
  </action>
  <verify>
    <automated>cd backend; npm test -- notifications.booking-status.e2e-spec.ts</automated>
  </verify>
  <done>Notification tests pass, demonstrating correct emission rules + idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe → API | Untrusted webhook requests; must be signature-verified + idempotent |
| API → Logs | Logs are an output channel; can leak PII if careless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | S (Spoofing) | Stripe webhook trigger for NOT-01 | mitigate | Reuse existing Stripe signature verification + idempotency record; emit only after confirmed transition applied. |
| T-05-07 | I (Info disclosure) | dev-email logs | mitigate | Log minimal transactional payload; avoid secrets/tokens; prefer bookingId + deepLink + coarse context only. |
| T-05-08 | R (Repudiation) | notification emission | accept | Dev-only logs sufficient; add deterministic log prefix and include bookingId/eventType for traceability. |
</threat_model>

<verification>
- Backend tests green: `cd backend; npm test -- notifications.booking-status.e2e-spec.ts`
</verification>

<success_criteria>
- Confirmed booking emits one dev-email log with deep link to booking detail
- Expired hold emits one dev-email log with deep link
- No emails emitted for creation or Stripe cancel return; duplicates idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/05-my-bookings-notifications/05-03-SUMMARY.md`
</output>

