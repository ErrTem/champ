---
phase: 01-platform-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/nest-cli.json
  - backend/tsconfig.json
  - backend/tsconfig.build.json
  - backend/prisma/schema.prisma
  - backend/src/main.ts
  - backend/src/app.module.ts
  - backend/src/prisma/prisma.module.ts
  - backend/src/prisma/prisma.service.ts
  - backend/src/health/health.module.ts
  - backend/src/health/health.controller.ts
  - backend/.env.example
  - backend/README.md
autonomous: true
requirements:
  - AUTH-01
user_setup:
  - Install PostgreSQL 15+ locally or Docker; set DATABASE_URL in backend/.env
must_haves:
  truths:
    - GET /health returns JSON with status ok and ISO timestamp
    - prisma schema defines User model with id email passwordHash createdAt updatedAt
    - npx prisma db push applies schema to DATABASE_URL without error
  artifacts:
    - backend/src/main.ts
    - backend/prisma/schema.prisma
  key_links:
    - Nest AppModule imports PrismaModule and HealthModule
---

<objective>
Create the NestJS backend workspace with Prisma + PostgreSQL, baseline User model (for signup in plan 02), JSON health endpoint, and documented environment variables including DATABASE_URL and future JWT/cookie secrets.

Purpose: Establishes the API process and database boundary before auth routes.

Output: Runnable `backend` package with `npm run start:dev` and successful schema push to Postgres.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-auth/01-CONTEXT.md
@.planning/phases/01-platform-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize NestJS workspace with Prisma</name>
  <files>backend/package.json,backend/nest-cli.json,backend/tsconfig.json,backend/tsconfig.build.json,backend/prisma/schema.prisma,backend/src/main.ts,backend/src/app.module.ts,backend/src/prisma/prisma.module.ts,backend/src/prisma/prisma.service.ts,backend/.env.example,backend/README.md</files>
  <read_first>
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
d:\programming\champ-app\.planning\phases\01-platform-auth\01-RESEARCH.md
d:\programming\champ-app\package.json
  </read_first>
  <action>
Create folder `backend/` at repo root. Scaffold NestJS 11 app (CLI: `nest new backend --package-manager npm --skip-git` or equivalent manual files matching Nest 11 defaults). Add dependencies: `@nestjs/config`, `@prisma/client`, `prisma` (dev), `class-validator`, `class-transformer`, `cookie-parser`, `@types/cookie-parser`. Add `prisma/schema.prisma` with datasource `postgresql` env `DATABASE_URL` and generator `client`. Define model `User` with fields: `id String @id @default(cuid())`, `email String @unique`, `passwordHash String`, `name String?`, `phone String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Implement `PrismaService` extending `PrismaClient` with `onModuleInit`/`enableShutdownHooks` per Nest docs. Wire `PrismaModule` as `@Global()` exporting `PrismaService`. In `main.ts` use `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true })` and `app.use(cookieParser())`. Add `backend/.env.example` with lines exactly: `DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/champ_dev`, `PORT=3000`, `NODE_ENV=development`, placeholder lines `JWT_ACCESS_SECRET=change-me-min-32-chars`, `JWT_REFRESH_SECRET=change-me-min-32-chars`. README section "Run" with `npm install`, `npx prisma generate`, `npm run start:dev`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
- File `backend/prisma/schema.prisma` contains model name `User` and field `passwordHash`
- File `backend/.env.example` contains substring `DATABASE_URL=postgresql://`
- Grep `PrismaService` in `backend/src/prisma/prisma.service.ts` returns match
  </acceptance_criteria>
  <done>Prisma schema validates; Nest boots with Prisma wired; .env.example documents DATABASE_URL.</done>
</task>

<task type="auto">
  <name>Task 2: Health module and CORS stub for Ionic dev origin</name>
  <files>backend/src/health/health.module.ts,backend/src/health/health.controller.ts,backend/src/app.module.ts,backend/src/main.ts</files>
  <read_first>
d:\programming\champ-app\backend\src\app.module.ts
d:\programming\champ-app\backend\src\main.ts
  </read_first>
  <action>
Add `HealthModule` registering `HealthController` at route prefix empty (controller `@Controller('health')`). `GET /health` returns body `{ "status": "ok", "timestamp": "<ISO8601>" }` using `new Date().toISOString()`. In `main.ts` enable CORS with `origin: ['http://localhost:8100','http://localhost:4200']`, `credentials: true`, `methods: ['GET','POST','PUT','PATCH','DELETE','OPTIONS']`. Import `ConfigModule.forRoot({ isGlobal: true })` in `AppModule`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `backend/src/health/health.controller.ts` contains `@Get()` and return object with key `status` value `ok`
- `backend/src/main.ts` contains `credentials: true` inside `enableCors` options
  </acceptance_criteria>
  <done>Production build succeeds; health route implemented.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push Prisma schema to database</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
d:\programming\champ-app\backend\prisma\schema.prisma
d:\programming\champ-app\backend\.env.example
  </read_first>
  <action>
With valid `DATABASE_URL` in `backend/.env` (create from .env.example if missing), run `npx prisma db push` from `backend/` directory. If non-interactive CI needs destructive accept, document in README; for local dev default push is sufficient. Record in README that executors must run this after any schema change.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx prisma db push</automated>
  </verify>
  <acceptance_criteria>
- Command `npx prisma db push` exits code 0 when run from `backend/` with valid DATABASE_URL
- `npx prisma studio` optional note in README — not required for verify
  </acceptance_criteria>
  <done>Live PostgreSQL contains `User` table matching Prisma schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Unauthenticated health checks; future auth bodies |
| API → PostgreSQL | Server-side only; credentials via env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | I | Health endpoint | accept | No sensitive data; rate-limit deferred to auth plan |
| T-1-02 | E | DATABASE_URL | mitigate | .env.example only placeholders; never commit .env |
| T-1-03 | T | Dev CORS origins | mitigate | Fixed localhost list; prod origins via env later |
</threat_model>

<verification>
Run `cd backend && npm run build` then `cd backend && npx prisma db push` then `cd backend && npm run start:dev` and `curl -s http://localhost:3000/health` contains `"status":"ok"`.
</verification>
