---
phase: 01-platform-auth
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/auth.service.ts
  - backend/src/auth/dto/request-reset.dto.ts
  - backend/src/auth/dto/confirm-reset.dto.ts
  - backend/README.md
autonomous: true
requirements:
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - POST /auth/forgot-password accepts email and always returns 200 with generic message (no enumeration)
    - When NODE_ENV=development, full reset URL or token logged to stdout per D-04
    - POST /auth/reset-password with valid token updates password and invalidates token
  artifacts:
    - backend/prisma/schema.prisma
    - backend/src/auth/auth.service.ts
  key_links:
    - PasswordResetToken (or equivalent) tied to User with expiry
---

<objective>
Implement secure password reset using one-time expiring tokens, with **development logging** of reset links per D-04 and README section documenting production email adapter swap (Resend/SendGrid/SES env keys).

Purpose: Satisfies AUTH-04 before client wires forgot-password UI.

Output: `/auth/forgot-password` and `/auth/reset-password` endpoints plus Prisma model for reset tokens.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-auth/01-CONTEXT.md
@.planning/phases/01-platform-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PasswordResetToken model and migration</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
d:\programming\champ-app\backend\prisma\schema.prisma
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
  </read_first>
  <action>
Add Prisma model `PasswordResetToken`: `id`, `userId`, `tokenHash String @unique`, `expiresAt DateTime`, `usedAt DateTime?`, relation to `User` with cascade delete. Run `npx prisma db push` from `backend/`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx prisma validate &amp;&amp; npx prisma db push</automated>
  </verify>
  <acceptance_criteria>
- Model `PasswordResetToken` exists in `schema.prisma`
- Field `usedAt` exists on `PasswordResetToken`
  </acceptance_criteria>
  <done>DB supports reset token lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Forgot and reset flows with dev logging</name>
  <files>backend/src/auth/auth.service.ts,backend/src/auth/auth.controller.ts,backend/src/auth/dto/request-reset.dto.ts,backend/src/auth/dto/confirm-reset.dto.ts,backend/README.md</files>
  <read_first>
d:\programming\champ-app\backend\src\auth\auth.service.ts
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
  </read_first>
  <action>
`forgotPassword(email)`: always schedule response `{ message: 'If an account exists, instructions were sent.' }` with HTTP 200. If user exists: create reset token (random 32 bytes), store SHA-256 in DB, `expiresAt = now() + 1h`. If `NODE_ENV === 'development'`, `console.log('[DEV ONLY] Password reset link: http://localhost:8100/reset-password?token=' + plaintextToken)` — log **plaintext token only in dev** per D-04. `resetPassword(token, newPassword)`: hash token, find row, check not used and not expired; bcrypt-hash new password; set `usedAt`; update user `passwordHash`; delete all refresh sessions for user. `POST /auth/forgot-password` body: `{ "email": string }`. `POST /auth/reset-password` body: `{ "token": string, "password": string }` with `@MinLength(8)` on password. README subsection **Email in production**: list env vars `EMAIL_PROVIDER`, `RESEND_API_KEY` as placeholders and state dev logging is disabled when `NODE_ENV=production`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `auth.service.ts` contains substring `[DEV ONLY]` for log message
- `auth.controller.ts` includes route handler for `forgot-password` OR method name `forgotPassword`
- README contains substring `RESEND_API_KEY` or `SendGrid`
  </acceptance_criteria>
  <done>Reset flows implemented with documented prod swap path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email inbox | User proves ownership via secret link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-08 | I | forgot-password | mitigate | Constant-time response; same JSON for missing email |
| T-1-09 | T | reset token | mitigate | One-time use; expiry 1h; hash at rest |
| T-1-10 | I | dev logs | mitigate | Guard logs with `NODE_ENV===development` only |
</threat_model>

<verification>
Set `NODE_ENV=development`, call forgot-password for seeded user, observe console contains `reset-password?token=`.
</verification>
