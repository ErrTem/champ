---
phase: 01-platform-auth
plan: 04
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - src/main.ts
  - src/app/app.routes.ts
  - src/app/core/config/api.config.ts
  - src/app/core/interceptors/auth.interceptor.ts
  - src/app/core/guards/auth.guard.ts
  - src/app/core/services/auth.service.ts
  - src/app/pages/login/login.page.ts
  - src/app/pages/login/login.page.html
  - src/app/pages/login/login.page.scss
  - src/app/pages/register/register.page.ts
  - src/app/pages/register/register.page.html
  - src/app/pages/register/register.page.scss
  - src/app/pages/forgot-password/forgot-password.page.ts
  - src/app/pages/forgot-password/forgot-password.page.html
  - src/app/pages/reset-password/reset-password.page.ts
  - src/app/pages/profile/profile.page.ts
  - src/app/pages/profile/profile.page.html
  - src/app/pages/home/home.page.ts
  - proxy.conf.json
  - angular.json
  - src/environments/environment.ts
  - src/environments/environment.development.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
user_setup:
  - Run backend on port 3000; configure Ionic dev proxy or `environment.apiUrl` to reach API; iOS Capacitor smoke per D-06 (checkpoint)
must_haves:
  truths:
    - User can register, login, logout from Ionic UI with credentials stored via httpOnly cookies (no access token in localStorage)
    - Browser refresh keeps session until logout (refresh cookie path works)
    - Forgot-password and reset-password pages call API; profile page loads and PATCHes name email phone
  artifacts:
    - src/app/core/services/auth.service.ts
    - src/app/app.routes.ts
  key_links:
    - HttpClient uses `withCredentials()` for all API calls to `apiUrl`
---

<objective>
Wire Ionic/Angular auth and profile flows to the Nest API: standalone pages (register, login, forgot-password, reset-password, profile), `HttpClient` with **withFetch** and **withCredentials**, optional dev **proxy** to avoid CORS friction, route guard protecting post-login area, and visual compliance with `01-UI-SPEC.md` / `DESIGN.md`.

Purpose: Delivers end-user AUTH-01..05 through the mobile-first UI; validates D-06 via explicit checkpoint.

Output: Runnable `ionic serve` against local API with complete auth loop.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-auth/01-UI-SPEC.md
@.planning/phases/01-platform-auth/01-CONTEXT.md
@DESIGN.md
@register.html
@login.html
@src/app/app.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: HttpClient, API URL, and auth session service</name>
  <files>src/main.ts,src/environments/environment.ts,src/environments/environment.development.ts,src/app/core/config/api.config.ts,src/app/core/services/auth.service.ts,src/app/core/interceptors/auth.interceptor.ts,proxy.conf.json,angular.json</files>
  <read_first>
d:\programming\champ-app\src\main.ts
d:\programming\champ-app\src\app\app.routes.ts
d:\programming\champ-app\angular.json
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
  </read_first>
  <action>
Add `provideHttpClient(withFetch(), withInterceptors([authInterceptor]))` in `main.ts` (import from `@angular/common/http`). Create `environment.ts` and `environment.development.ts` with `apiUrl: 'http://localhost:3000'` (or `/api` if using proxy — if proxy, set `apiUrl: '/api'` and add `proxy.conf.json` mapping `/api` → `http://localhost:3000` with `changeOrigin: true`, wire `proxyConfig` in `angular.json` serve options). Implement `AuthService` with signals or `rxjs` for `user` and `authenticated` derived from `GET /users/me` (implemented in plan 02 `UsersController`). Methods: `register`, `login`, `logout`, `refreshSession`, `loadProfile` — all use `http.post/get` with `{ withCredentials: true }`. **Do not** store JWT in `localStorage`. Interceptor: on 401 from API, attempt single `POST /auth/refresh` then retry original request once (no infinite loop).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
- `src/main.ts` contains `provideHttpClient` and `withFetch`
- `auth.service.ts` contains substring `withCredentials` OR `withCredentials: true`
- File `src/app/core/services/auth.service.ts` does not contain the substring `localStorage`
  </acceptance_criteria>
  <done>Client sends credentials; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Auth routes, guard, and transactional pages</name>
  <files>src/app/app.routes.ts,src/app/core/guards/auth.guard.ts,src/app/pages/login/login.page.ts,src/app/pages/login/login.page.html,src/app/pages/login/login.page.scss,src/app/pages/register/register.page.ts,src/app/pages/register/register.page.html,src/app/pages/register/register.page.scss,src/app/pages/forgot-password/forgot-password.page.ts,src/app/pages/forgot-password/forgot-password.page.html,src/app/pages/reset-password/reset-password.page.ts,src/app/pages/reset-password/reset-password.page.html,src/app/pages/profile/profile.page.ts,src/app/pages/profile/profile.page.html,src/app/pages/profile/profile.page.scss</files>
  <read_first>
d:\programming\champ-app\.planning\phases\01-platform-auth\01-UI-SPEC.md
d:\programming\champ-app\register.html
d:\programming\champ-app\login.html
d:\programming\champ-app\src\app\home\home.page.ts
  </read_first>
  <action>
Extend `app.routes.ts`: paths `login`, `register`, `forgot-password`, `reset-password` lazy-loaded; `profile` lazy-loaded with `canActivate: [authGuard]` using functional guard calling `AuthService.loadProfile()` or checking auth signal. Default redirect: if unauthenticated user hits `home`, allow; optional send to `login` for protected shell — per product choice use **profile as guarded hub** and keep `home` public. Implement pages with Ionic components (`ion-header` TopAppBar glass per UI-SPEC, `ion-content`, `ion-input`, `ion-button` primary gradient). Forms: register (email, password, confirm), login (email, password), forgot (email), reset (token from query param `token`, new password, confirm), profile (name, email, phone) with `PATCH /users/me`. Wire navigation links between login/register/forgot. Styling: use CSS variables aligned to DESIGN.md tokens (`--ion-color-primary` mapping or global styles in `src/global.scss` if needed).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
- `app.routes.ts` contains path string `'profile'` with `canActivate`
- `profile.page.ts` exists under `src/app/pages/profile/`
- `register.page.ts` calls `AuthService.register` or HTTP post to `/auth/register`
  </acceptance_criteria>
  <done>All auth screens routable; guard blocks anonymous profile access.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: D-06 iOS / Capacitor auth persistence smoke</name>
  <files>README.md</files>
  <read_first>
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
  </read_first>
  <action>
Document in root `README.md` (or `docs/ios-auth.md` only if README already crowded — prefer **append section to README** "iOS auth verification") checklist: (1) `ionic capacitor sync ios`, (2) open Xcode workspace, (3) sign in on simulator, (4) force-quit app, relaunch, confirm still authenticated, (5) repeat on physical device if available. Record actual `API_URL`/cookie/`SameSite` settings used. If refresh fails on device, capture WKWebView console errors and file follow-up issue — do not block merge on device-only failure but document result.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
- README.md at repo root contains substrings `iOS` and `auth` in the same added section (title may be `## iOS auth` or similar)
- Human confirms checklist step 3–4 attempted (simulator minimum)
  </acceptance_criteria>
  <done>Checkpoint satisfied with documented manual result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser WebView → API | PII in profile forms |
| Angular bundle | XSS could exfil cookies if not httpOnly — mitigated by httpOnly flag server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | S | Profile PATCH | mitigate | Server validates ownership via JWT `sub` |
| T-1-12 | X | Templates | mitigate | Angular sanitization; avoid `innerHTML` with user data |
| T-1-13 | T | CSRF | accept L1 | SameSite=Lax cookies reduce risk; document CSRF token follow-up if cookie auth without SameSite strict |
</threat_model>

<verification>
`npm run build` succeeds; manual: login in browser, refresh page, still on profile; logout clears session.
</verification>
