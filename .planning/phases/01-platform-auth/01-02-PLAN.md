---
phase: 01-platform-auth
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/auth/auth.module.ts
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/auth.service.ts
  - backend/src/auth/dto/login.dto.ts
  - backend/src/auth/dto/register.dto.ts
  - backend/src/auth/strategies/jwt-access.strategy.ts
  - backend/src/auth/guards/jwt-access.guard.ts
  - backend/src/users/users.module.ts
  - backend/src/users/users.service.ts
  - backend/src/users/users.controller.ts
  - backend/package.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
user_setup: []
must_haves:
  truths:
    - POST /auth/register with unique email returns 201 and Set-Cookie headers for access and refresh cookies
    - POST /auth/login valid credentials returns 200 and cookies; invalid returns 401
    - POST /auth/refresh with valid refresh cookie returns new cookies; reuse of old refresh invalidated when rotation enabled
    - POST /auth/logout clears auth cookies (Max-Age=0 or empty value per Nest cookie API)
    - GET /users/me returns { id, email, name, phone } for authenticated user; PATCH /users/me updates name/email/phone for owner only
  artifacts:
    - backend/src/auth/auth.controller.ts
    - backend/src/auth/auth.service.ts
  key_links:
    - JwtAccessStrategy reads Bearer or cookie named `access_token` (document chosen approach in code comments — must match Ionic client in plan 04)
---

<objective>
Implement user registration, login, refresh rotation, and logout using bcrypt password hashing, JWT access tokens, opaque refresh tokens stored in PostgreSQL, and httpOnly Secure SameSite cookies per D-01/D-02.

Purpose: Delivers core AUTH-01..03 server semantics the Ionic app will call in plan 04.

Output: Working auth REST endpoints under `/auth/*` with integration-ready cookie names documented in `backend/README.md`.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-auth/01-CONTEXT.md
@.planning/phases/01-platform-auth/01-RESEARCH.md
@.planning/phases/01-platform-auth/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma models for refresh sessions</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
d:\programming\champ-app\backend\prisma\schema.prisma
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
  </read_first>
  <action>
Extend Prisma schema with `RefreshSession` model: `id String @id @default(cuid())`, `userId String`, `tokenHash String @unique`, `expiresAt DateTime`, `createdAt DateTime @default(now())`, relation `user User @relation(fields: [userId], references: [id], onDelete: Cascade)`. Add back-relation on `User` as `refreshSessions RefreshSession[]`. After edit run `[BLOCKING]` `npx prisma db push` from `backend/` (same as plan 01 task 3 pattern).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx prisma validate &amp;&amp; npx prisma db push</automated>
  </verify>
  <acceptance_criteria>
- `backend/prisma/schema.prisma` contains model `RefreshSession`
- `RefreshSession` includes fields `tokenHash` and `expiresAt`
  </acceptance_criteria>
  <done>Schema migrated; DB has refresh session storage.</done>
</task>

<task type="auto">
  <name>Task 2: AuthService — register, login, refresh, logout</name>
  <files>backend/src/auth/auth.service.ts,backend/src/auth/dto/register.dto.ts,backend/src/auth/dto/login.dto.ts,backend/src/users/users.service.ts,backend/package.json</files>
  <read_first>
d:\programming\champ-app\backend\src\prisma\prisma.service.ts
d:\programming\champ-app\.planning\phases\01-platform-auth\01-CONTEXT.md
  </read_first>
  <action>
Add `bcrypt` with cost factor **12**. `register(dto)`: reject duplicate email with 409; hash password; create `User`. `login(dto)`: compare bcrypt; on success create refresh session record storing **SHA-256 hash** of random 32-byte token (use `crypto.randomBytes`); sign **access JWT** with `@nestjs/jwt` using secret `JWT_ACCESS_SECRET`, payload `{ sub: userId, email }`, expiry **900s** (15 minutes). `refresh`: read refresh token from httpOnly cookie named `refresh_token`; hash lookup; verify not expired; **rotate** (delete old session row, issue new); return new access + refresh Set-Cookie headers. `logout`: delete refresh session by token hash from cookie; clear both cookies. Use `argon2` is NOT allowed — stick bcrypt per RESEARCH. Document cookie names: `access_token`, `refresh_token`; options: `httpOnly: true`, `sameSite: 'lax'`, `secure: process.env.NODE_ENV === 'production'`, `path: '/'`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `auth.service.ts` imports `bcrypt` and `JwtService`
- `register.dto.ts` uses `class-validator` decorators `@IsEmail()` and `@MinLength(8)` on password
- Grep in `auth.service.ts` for `refresh_token` cookie name returns at least one match
  </acceptance_criteria>
  <done>Auth business logic compiles; rotation and hashing implemented in code.</done>
</task>

<task type="auto">
  <name>Task 3: AuthController routes and JWT guard</name>
  <files>backend/src/auth/auth.controller.ts,backend/src/auth/auth.module.ts,backend/src/auth/strategies/jwt-access.strategy.ts,backend/src/auth/guards/jwt-access.guard.ts,backend/src/users/users.module.ts,backend/src/app.module.ts</files>
  <read_first>
d:\programming\champ-app\backend\src\auth\auth.service.ts
d:\programming\champ-app\backend\src\main.ts
  </read_first>
  <action>
Wire `AuthModule` importing `UsersModule`, `JwtModule.registerAsync` reading `JWT_ACCESS_SECRET` and `signOptions: { expiresIn: '900s' }`. `AuthController`: `POST /auth/register`, `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout` — all return JSON `{ ok: true }` or user DTO without password fields. Use `@Res({ passthrough: true })` pattern to set cookies via `res.cookie`. Implement `JwtAccessStrategy` with `passport-jwt` extracting JWT from **Authorization Bearer** header OR from `access_token` cookie using `ExtractJwt.fromExtractors([...])`. Export `JwtAccessAuthGuard` for reuse in profile plan. Register global prefix empty. Ensure `UsersService` exposes `findByEmail`, `findById`, `updateProfile(userId, dto)`. Add `UsersController` at `@Controller('users')` with `GET /me` (`@Get('me')`) returning safe user fields, guarded by `JwtAccessAuthGuard`; add `PATCH /me` accepting `{ name?, email?, phone? }` with validation — email unique check on change; only owning user per JWT `sub` (AUTH-05 / D-05).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create backend/test/auth.e2e-spec.ts before this verify passes; until then: cd backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `auth.controller.ts` contains string `register` in decorator path and `auth/register` route OR `@Post('register')` inside `@Controller('auth')`
- `jwt-access.strategy.ts` extends `PassportStrategy(Strategy, 'jwt-access')` or equivalent Strategy from passport-jwt
- `users.controller.ts` contains `@Get('me')` and `@Patch('me')`
- `auth.controller.ts` contains `@Post('login')` or path `login`
  </acceptance_criteria>
  <done>All four endpoints exposed; guard ready for profile routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → /auth/* | Credential submission |
| Auth service → DB | Refresh token hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T-1-02 | S | /auth/login | mitigate | bcrypt; generic 401 on failure (no user enumeration in message text) |
| T-1-04 | T | JWT | mitigate | Strong `JWT_ACCESS_SECRET` (32+ chars); short TTL 900s |
| T-1-05 | R | /auth/refresh | mitigate | One-time rotation; invalidate reused refresh if detected |
| T-1-06 | E | Cookies | mitigate | httpOnly + Secure in prod + SameSite=Lax |
| T-1-07 | D | User table | mitigate | Prisma parameterized queries only |
</threat_model>

<verification>
Manual: `curl -i -X POST http://localhost:3000/auth/register -H "Content-Type: application/json" -d "{\"email\":\"a@b.com\",\"password\":\"password123\"}"` returns Set-Cookie lines containing `access_token` and `refresh_token`.
</verification>
