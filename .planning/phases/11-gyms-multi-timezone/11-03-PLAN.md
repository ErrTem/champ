---
phase: 11-gyms-multi-timezone
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  # Fighter profile UI
  - src/app/pages/fighter-profile/fighter-profile.page.ts
  - src/app/pages/fighter-profile/fighter-profile.page.html
  - src/app/pages/fighter-profile/fighter-profile.page.scss
  # Map link helper + tests (Wave 0)
  - src/app/shared/utils/map-links.ts
  - src/app/shared/utils/map-links.spec.ts
autonomous: true
requirements:
  - R7
user_setup: []

must_haves:
  truths:
    - "Fighter profile renders gym address and user can open map from button"
    - "Map link generation uses safe URL encoding and cannot produce unsafe schemes"
    - "UI uses explicit API-provided gym + timezone fields (no guessing)"
  artifacts:
    - path: "src/app/pages/fighter-profile/fighter-profile.page.html"
      provides: "Gym address block + map button"
    - path: "src/app/shared/utils/map-links.ts"
      provides: "Safe Google/Apple maps URL builder"
    - path: "src/app/shared/utils/map-links.spec.ts"
      provides: "URL encoding + safety coverage"
  key_links:
    - from: "src/app/pages/fighter-profile/fighter-profile.page.ts"
      to: "map-links.ts"
      via: "build encoded map URL"
      pattern: "build(Google|Apple)MapsUrl|map-links"
    - from: "src/app/pages/fighter-profile/fighter-profile.page.ts"
      to: "backend fighter profile DTO"
      via: "gym fields used for rendering"
      pattern: "gym\\.|gymAddress|timezone"
---

<objective>
Frontend deliverables for Phase 11:
- Show gym address on fighter profile.
- Add “Show on map” action using canonical Google Maps URL and iOS-preferred Apple Maps URL (fallback Google).
- Ensure map URLs are built safely with URL encoding (no `javascript:` or broken query strings).

Purpose: users can see where gym is and open maps reliably; avoids injection/broken links.
Output: fighter profile UI changes + shared map-link helper + Karma/Jasmine unit specs (Wave 0).
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-gyms-multi-timezone/11-CONTEXT.md
@.planning/phases/11-gyms-multi-timezone/11-RESEARCH.md
@.planning/phases/11-gyms-multi-timezone/11-VALIDATION.md
@.planning/REQUIREMENTS.md

Locked decisions:
- D-03 Maps links: web Google Maps; iOS prefer Apple Maps URL when available (fallback Google).
- DTO policy (from research + requirement): DTO includes `gym` and `timezone` explicitly where UI needs.

Risk notes (must account for during execution):
- URL encoding: addresses contain `&`, `#`, unicode. Build URLs via `URL`/`URLSearchParams` (or `encodeURIComponent`) and unit-test output.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 / 11-03-01): Add frontend unit specs for safe map-link building + URL encoding</name>
  <files>
src/app/shared/utils/map-links.ts,
src/app/shared/utils/map-links.spec.ts
  </files>
  <behavior>
    - Given structured address fields, builder returns:
      - Google: `https://www.google.com/maps/search/?api=1&query=<encoded>`
      - Apple: `http://maps.apple.com/?q=<encoded>`
    - Encoding:
      - Address containing `&`, `?`, `#`, unicode is encoded and does not break URL.
    - Safety:
      - No output can start with `javascript:` or contain unencoded newlines.
      - Builder never accepts an arbitrary URL string; only address components.
  </behavior>
  <action>
Create `src/app/shared/utils/map-links.ts` exporting small pure functions:
- `formatGymAddress(gym): string` (single-line string used as query)
- `buildGoogleMapsSearchUrl(query: string): string` using `URL` + `URLSearchParams` (or `encodeURIComponent`) so encoding correct.
- `buildAppleMapsUrl(query: string): string` likewise.

Write `src/app/shared/utils/map-links.spec.ts` (Karma/Jasmine):
- Tests for encoding (special chars stay in `query=` encoded form).
- Tests for "no javascript:" output (assert URL starts with expected base).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Map link helper exists and unit specs run green.</done>
</task>

<task type="auto">
  <name>Task 2: Render gym address + map button on fighter profile; open correct URL per platform</name>
  <files>
src/app/pages/fighter-profile/fighter-profile.page.ts,
src/app/pages/fighter-profile/fighter-profile.page.html,
src/app/pages/fighter-profile/fighter-profile.page.scss
  </files>
  <action>
UI rendering:
- In fighter profile page, locate where fighter details render.
- Add "Gym" section showing:
  - gym name
  - formatted address (line breaks ok)
- Add button "Show on map".

Map open behavior (D-03):
- Determine platform:
  - If app running on iOS (Capacitor/Ionic platform detection if already in repo), use Apple Maps URL `http://maps.apple.com/?q=...`.
  - Otherwise use Google Maps search URL `https://www.google.com/maps/search/?api=1&query=...`.
- Use helper from `src/app/shared/utils/map-links.ts` for query building and URL creation.
- Open link using existing Ionic/Angular pattern in repo (e.g., `window.open(url, '_blank')` or Capacitor Browser plugin if already used). No new dependencies.

DTO usage:
- Consume `fighter.gym` fields from backend DTO (Plan 01) and do not reconstruct timezone/address from other fields.
- Ensure template guards null/undefined so profile page doesn't crash during loading.

Timezone display policy (R7 acceptance safety):
- Any time values rendered on fighter profile / book flow MUST be formatted using API-provided timezone (`fighter.gym.timezone` or `availability.timezone`) rather than device local timezone.
- If fighter profile shows any upcoming slot/booking time, format using that timezone and (optionally) show small label like `America/New_York` for clarity.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - Fighter profile shows gym address block.
    - Map button opens Apple Maps on iOS, else Google Maps.
    - Address is URL-encoded (spaces, special chars) and link works for addresses with punctuation.
    - Any displayed times use gym timezone (not device-local).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API → UI | gym address fields are displayed and used to build outbound URL |
| UI → external maps site/app | encoded address query becomes part of external URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-07 | T | map URL injection via address fields | mitigate | never store/accept arbitrary URLs; build canonical URLs via encoding; unit tests enforce base URL + no javascript scheme |
| T-11-08 | I | address displayed incorrectly | accept | address is intended to be user-visible; no secrets; ensure formatting clean |
</threat_model>

<verification>
Automated (per `11-VALIDATION.md`):
- `npm test`

Manual smoke (part of phase sign-off):
- Open fighter profile with gym containing special chars in address (e.g., "123 Main St #5 & 2nd Ave") and click map button; maps opens and query shows full address.
</verification>

<success_criteria>
- Profile page shows gym address and opens maps link reliably.
- Map URLs always safely encoded and use canonical Google/Apple formats.
- Frontend unit specs cover encoding + scheme safety.
</success_criteria>

<output>
After completion, create `.planning/phases/11-gyms-multi-timezone/11-03-SUMMARY.md`
</output>
