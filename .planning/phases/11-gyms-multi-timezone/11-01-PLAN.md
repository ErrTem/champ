---
phase: 11-gyms-multi-timezone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # DB + domain
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/*/migration.sql
  - backend/src/gyms/gyms.module.ts
  - backend/src/gyms/gyms.service.ts
  - backend/src/gyms/dto/gym.dto.ts
  - backend/src/fighters/fighters.service.ts
  - backend/src/fighters/dto/fighter-profile.dto.ts
  - backend/src/fighters/dto/fighter-list-item.dto.ts
  - backend/prisma/seed.ts
  # Tests (Wave 0)
  - backend/src/gyms/gyms.service.spec.ts
  - backend/test/gyms.migration-backfill.e2e-spec.ts
autonomous: true
requirements:
  - R7
user_setup: []

must_haves:
  truths:
    - "Gym entity exists with address + IANA timezone and fighters belong to exactly one gym"
    - "Existing fighters migrated/backfilled to a default gym (timezone America/Los_Angeles) without changing existing schedule semantics"
    - "Fighter DTOs include gym + timezone explicitly where UI needs"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Gym model + Fighter.gymId relation"
    - path: "backend/prisma/migrations/*/migration.sql"
      provides: "Default gym insert + fighters backfill"
    - path: "backend/src/gyms/gyms.service.ts"
      provides: "Timezone validation + default gym helper"
    - path: "backend/src/fighters/dto/fighter-profile.dto.ts"
      provides: "Fighter profile includes gym + timezone"
    - path: "backend/test/gyms.migration-backfill.e2e-spec.ts"
      provides: "Backfill/default gym behavior automated"
  key_links:
    - from: "backend/src/fighters/fighters.service.ts"
      to: "Gym.timezone"
      via: "include gym relation and map to DTO"
      pattern: "include:\\s*\\{[\\s\\S]*gym"
    - from: "backend/prisma/migrations/*/migration.sql"
      to: "Fighter.gymId"
      via: "UPDATE Fighter SET gymId ="
      pattern: "UPDATE\\s+\"Fighter\"[\\s\\S]*\"gymId\""
---

<objective>
Introduce gyms domain and default backfill:
- Add `Gym` model with structured address + IANA timezone (source of truth).
- Add required `Fighter.gymId` relation (each fighter exactly 1 gym).
- Migration/backfill: create default gym and assign all existing fighters to it.
- Update backend DTOs so UI sees `gym` and `timezone` explicitly (no guessing).

Purpose: establish gym/timezone foundation for Phase 11 timezone rewiring + UI rendering.
Output: Prisma migration + gyms module/service + updated fighter DTOs + Wave 0 automated specs.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-gyms-multi-timezone/11-CONTEXT.md
@.planning/phases/11-gyms-multi-timezone/11-RESEARCH.md
@.planning/phases/11-gyms-multi-timezone/11-VALIDATION.md

Locked decisions to honor (from `11-CONTEXT.md`):
- D-01 Timezone source of truth: `Gym.timezone` (IANA).
- D-02 Fighter ↔ gym: each fighter belongs to exactly 1 gym; existing fighters backfilled to default gym.
- D-03 Maps links policy implemented in frontend plan (Plan 03).

Risk notes (must account for during execution):
- Data migration: `Fighter.gymId` becomes required. Migration MUST create default gym and backfill all existing fighters in same migration so deploy/dev DB never ends in invalid state.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 / 11-01-01): Add backend specs for timezone validation + default gym/backfill invariants</name>
  <files>
backend/src/gyms/gyms.service.spec.ts,
backend/test/gyms.migration-backfill.e2e-spec.ts
  </files>
  <behavior>
    - Timezone validation:
      - Accepts valid IANA zones: `America/Los_Angeles`, `America/New_York`
      - Rejects invalid zones: `GMT+3`, `Mars/OlympusMons`, empty, whitespace
    - Default gym/backfill:
      - There exists exactly 1 default gym used for migration/backfill, timezone `America/Los_Angeles`
      - All fighters in DB have non-null `gymId`
  </behavior>
  <action>
Create unit spec `backend/src/gyms/gyms.service.spec.ts` for the validation function used by create/update gym flows (implement in Task 2/3).

Create e2e spec `backend/test/gyms.migration-backfill.e2e-spec.ts`:
- Use existing backend e2e harness pattern.
- Assert post-migration invariants (default gym exists, fighters have gymId). This test can assume migrations already applied in test DB (still automated) if harness cannot apply migrations on the fly.
  </action>
  <verify>
    <automated>cd backend && npm test -- gyms.service.spec.ts gyms.migration-backfill.e2e-spec.ts</automated>
  </verify>
  <done>Wave 0 specs exist and run under Jest.</done>
</task>

<task type="auto">
  <name>Task 2: Add `Gym` model + `Fighter.gymId` relation + default gym migration/backfill (D-01, D-02)</name>
  <files>
backend/prisma/schema.prisma,
backend/prisma/migrations/*/migration.sql
  </files>
  <action>
Update Prisma schema:
- Add `model Gym` with: `id`, `name`, structured address fields (`addressLine1`, `addressLine2?`, `city`, `state`, `postalCode`, `countryCode` default "US"), `timezone` (string), timestamps.
- Add required relation: `Fighter.gymId` (String, required) + `Fighter.gym` relation; `Gym.fighters` back relation.

Migration/backfill (must be included, per D-02):
- Create a Prisma migration (execution should run `cd backend && npx prisma migrate dev --name gyms`).
- Ensure migration includes:
  - Insert default gym row with `timezone='America/Los_Angeles'`.
  - Backfill all existing fighters to that default gym.
- Prefer a deterministic default gym id in SQL (cuid cannot be generated in pure SQL easily). Options:
  - Use a fixed UUID string as `Gym.id` in migration and set schema id default to `uuid()` (only if codebase already uses uuid), OR
  - Insert default gym, then update fighters using subquery `SELECT id FROM "Gym" WHERE name='Default Gym' LIMIT 1`.
- Use one of these patterns so migration is self-contained and repeatable in dev.
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate dev</automated>
  </verify>
  <done>
    - `Gym` table exists with `timezone` field.
    - `Fighter.gymId` is non-null for existing rows after migration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add gyms backend module/service + update fighter DTOs to include gym + timezone explicitly (D-01)</name>
  <files>
backend/src/gyms/gyms.module.ts,
backend/src/gyms/gyms.service.ts,
backend/src/gyms/dto/gym.dto.ts,
backend/src/fighters/fighters.service.ts,
backend/src/fighters/dto/fighter-profile.dto.ts,
backend/src/fighters/dto/fighter-list-item.dto.ts,
backend/prisma/seed.ts
  </files>
  <action>
Gyms service:
- Create `backend/src/gyms/gyms.service.ts` exposing:
  - `assertValidTimezone(timezone: string)` (or similar) using Luxon IANA validation (per research; pick actual Luxon API during execution).
  - `getDefaultGym()` helper for seed/tests (find by name "Default Gym").

DTOs (API contract change, explicit gym + timezone where UI needs):
- Update fighter DTOs to include gym fields explicitly:
  - `FighterProfileDto` MUST include `gym: { id, name, timezone, addressLine1, addressLine2?, city, state, postalCode, countryCode }`
  - If list items used in UI need timezone, include `gym` there too; otherwise keep list minimal but do not hide timezone where UI displays times.
- Update `FightersService` methods that build DTOs to `include: { gym: true }` and map to DTO shape.

Seed (`backend/prisma/seed.ts`):
- Ensure seed always creates/updates default gym row (same name + tz) and assigns seeded fighters to a gym.
- Keep idempotent upsert style used elsewhere.
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <done>
    - Backend DTO responses include `gym` object with `timezone` for fighter profile.
    - Timezone validation helper exists and is covered by unit spec.
    - Default gym exists via migration and also present in seed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Admin/seed/migration → DB | migration writes default gym + updates fighters |
| API → DB (Prisma) | gym + fighter reads join across relations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-01 | T | `Gym.timezone` input | mitigate | validate IANA timezone server-side (Luxon zone validation) and reject invalid values |
| T-11-02 | R | migration/backfill correctness | mitigate | automated invariant test ensures default gym exists + fighters have non-null gymId |
| T-11-03 | I | leaking unexpected fields in DTO | mitigate | map explicit DTO shape; do not return raw Prisma entities |
</threat_model>

<verification>
Automated (per `11-VALIDATION.md`):
- `cd backend && npm test`

Manual smoke (later, after Plan 02/03 ship):
- Seed 2 gyms (NY/LA), 2 fighters, browse profile + booking times (handled in later plans).
</verification>

<success_criteria>
- `Gym` exists with structured address + `timezone` (IANA).
- `Fighter.gymId` required and backfilled for existing data (default gym timezone America/Los_Angeles).
- Fighter DTOs include `gym` + `timezone` explicitly for UI use.
- Wave 0 tests exist and run green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-gyms-multi-timezone/11-01-SUMMARY.md`
</output>
