---
phase: 14-social-sharing
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/app/core/models/catalog.models.ts
  - src/app/core/services/catalog.service.ts
  - src/app/core/services/share.service.ts
  - src/app/core/utils/url-safety.ts
  - src/app/core/utils/url-safety.spec.ts
  - src/app/pages/fighter-profile/fighter-profile.page.ts
  - src/app/pages/fighter-profile/fighter-profile.page.html
autonomous: false
requirements:
  - R10
must_haves:
  truths:
    - "Fighter profile renders social links only when present"
    - "Rendered social links never allow javascript: (XSS-safe href)"
    - "Share action always offers copy-link; uses share sheet when available"
    - "Shared link uses canonical route /explore/fighters/:fighterId"
  artifacts:
    - path: "src/app/core/services/share.service.ts"
      provides: "Capability-detected share/copy adapter"
      min_lines: 30
    - path: "src/app/core/utils/url-safety.ts"
      provides: "Safe URL validator/normalizer for href"
      exports: ["isSafeHttpUrl", "coerceSafeHttpUrl"]
    - path: "src/app/pages/fighter-profile/fighter-profile.page.html"
      provides: "Conditional social links + share button UI"
      contains: "Share"
  key_links:
    - from: "src/app/pages/fighter-profile/fighter-profile.page.ts"
      to: "src/app/core/services/share.service.ts"
      via: "inject + method call"
      pattern: "inject\\(.*ShareService\\)"
    - from: "src/app/pages/fighter-profile/fighter-profile.page.html"
      to: "src/app/core/utils/url-safety.ts"
      via: "precomputed safe href values"
      pattern: "href"
---

<objective>
Add share action + optional social links rendering to fighter profile (R10), with strict href safety to prevent XSS.

Purpose: enable users to share canonical fighter profile link and visit fighter social pages safely.
Output: ShareService + URL safety util + fighter profile UI changes + unit coverage + validation checkpoint.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-social-sharing/14-CONTEXT.md
@.planning/phases/14-social-sharing/14-RESEARCH.md

Canonical fighter profile route (locked by context, proven in code):
- `src/app/app.routes.ts`: `explore/fighters/:fighterId`
- Legacy redirect exists: `fighters/:fighterId` -> `explore/fighters/:fighterId`

Current fighter profile page:
- `src/app/pages/fighter-profile/fighter-profile.page.ts`
- `src/app/pages/fighter-profile/fighter-profile.page.html`

Service pattern:
- `src/app/core/services/*.ts` uses `@Injectable({ providedIn: 'root' })` + `inject(...)`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add URL safety utility + tests (allowlist schemes)</name>
  <files>src/app/core/utils/url-safety.ts, src/app/core/utils/url-safety.spec.ts</files>
  <behavior>
    - Accept only absolute `https:` URLs (optionally `http:` for localhost/dev if needed).
    - Reject `javascript:`, `data:`, `vbscript:`, relative URLs, and malformed URLs.
    - Return either `null`/`undefined` for unsafe input or a normalized safe string.
  </behavior>
  <action>
    Create `url-safety.ts` exporting helpers used by templates/components (not Angular DomSanitizer bypass):
    - `isSafeHttpUrl(input: string): boolean` (scheme allowlist + URL parse)
    - `coerceSafeHttpUrl(input: string | null | undefined): string | null` (trim + validate; return normalized string or null)

    Add unit tests in `url-safety.spec.ts` covering:
    - `https://example.com` => allowed
    - `javascript:alert(1)` => rejected
    - `data:text/html,...` => rejected
    - `//example.com` (scheme-relative) => rejected
    - `https://example.com/%0Ajavascript:...` => rejected (ensure URL parser normalizes and still https)
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    URL safety utility exists, covered by unit tests, and blocks scheme-based XSS vectors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ShareService (navigator.share + copy fallback)</name>
  <files>src/app/core/services/share.service.ts</files>
  <behavior>
    - If `navigator.share` available, call it with `{ title?, text?, url }`.
    - Always provide copy-link fallback that works on web/PWA: use `navigator.clipboard.writeText` when available; else fallback to textarea+execCommand copy.
    - Share/copy returns a result the UI can use (success/failure) without throwing unhandled errors.
  </behavior>
  <action>
    Create `ShareService` with single public method (example signature; adjust as needed):
    - `shareOrCopy(payload: { title?: string; text?: string; url: string }): Promise<{ method: 'share' | 'copy'; ok: boolean }>`

    Implementation rules:
    - Capability detect `navigator.share` (and optionally `navigator.canShare` if you pass files; here only url/text so keep simple).
    - If share fails (throws), fall back to copy.
    - For copy, prefer `navigator.clipboard.writeText` (secure context); else DOM fallback.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    ShareService supports share sheet when available and copy fallback always.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render social links + share button on fighter profile using canonical URL</name>
  <files>src/app/core/models/catalog.models.ts, src/app/core/services/catalog.service.ts, src/app/pages/fighter-profile/fighter-profile.page.ts, src/app/pages/fighter-profile/fighter-profile.page.html</files>
  <behavior>
    - Fighter profile model includes optional `instagramUrl`, `facebookUrl`, `xUrl` (or twitterUrl) as `string | null`.
    - UI renders social link buttons/anchors only when corresponding safe URL exists.
    - Clicking share triggers ShareService with canonical URL: `${window.location.origin}/explore/fighters/${fighterId}`.
    - Web/PWA always supports copy-link minimum.
  </behavior>
  <action>
    Data/model wiring:
    - Update `FighterProfile` type in `catalog.models.ts` to include optional social link fields (string|null).
    - Update `MOCK_FIGHTERS` objects in `catalog.service.ts` to include these fields (null/undefined ok) to keep mock mode compiling.

    UI wiring:
    - In `fighter-profile.page.ts`, inject `ShareService`.
    - Add a `shareProfile()` method:
      - Pull `fighterId` from route param.
      - Build canonical path `/explore/fighters/${encodeURIComponent(fighterId)}`.
      - Use `window.location.origin` to create absolute URL for sharing/copy.
    - Precompute safe social hrefs in TS (recommended) using `coerceSafeHttpUrl` so template stays simple and never binds unsafe strings to `[href]`.

    Template updates in `fighter-profile.page.html`:
    - Add a "Share" button somewhere in hero or services header.
    - Add social links row/section (Instagram / Facebook / X) only if safe href exists.
    - Use normal `<a target="_blank" rel="noopener noreferrer">` for external links.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Fighter profile shows social links only when set, blocks unsafe URLs from rendering, and share button copies/shares canonical link.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Social links rendering + Share button on fighter profile with capability detection and copy fallback.
  </what-built>
  <how-to-verify>
    1) Start app in browser (`npm start`), open any fighter profile (from Explore list).
    2) Confirm Share button exists; click it:
       - If browser supports share sheet: share UI opens, cancel ok.
       - Otherwise: link copied to clipboard (paste into address bar/text field).
    3) Confirm copied/shared URL equals `{origin}/explore/fighters/{fighterId}` (canonical).
    4) Seed/set a fighter with social URLs (admin endpoint or DB), reload profile:
       - Social link buttons appear only for provided fields.
       - Clicking opens correct site in new tab.
    5) Negative: set social url to `javascript:alert(1)` server-side, reload:
       - No clickable link rendered for that field.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what broke</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| API → UI template | Untrusted strings potentially bound into DOM `href` |
| UI action → Web APIs | Share/copy interacts with browser capabilities + permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-14-04 | E | `href` rendering in fighter profile | mitigate | Allowlist URL schemes via `url-safety.ts`; never bind raw backend strings to `href` |
| T-14-03 | A | Share capability detection | mitigate | Prefer `navigator.share` when present, else copy fallback; catch+fallback on errors |
| T-14-02 | T | Canonical URL building | mitigate | Build from known route `explore/fighters/:fighterId`; unit tests cover builder + UI manual check |
</threat_model>

<verification>
- Frontend unit tests pass: `npm test`.
- Manual checkpoint passes (share button behavior + safe links).
</verification>

<success_criteria>
- Social links appear only when present and safe; unsafe inputs never become clickable `href`.
- Share action always yields usable link (copy minimum) and uses share sheet when supported.
- Shared URL uses canonical route `/explore/fighters/:fighterId`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-social-sharing/14-02-SUMMARY.md`
</output>

