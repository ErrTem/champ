---
phase: 14-social-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/fighters/fighters.service.ts
  - backend/src/fighters/dto/fighter-profile.dto.ts
  - backend/src/admin/fighters.admin.service.ts
  - backend/src/admin/dto/admin-fighter.dto.ts
  - backend/test/admin.fighters.e2e-spec.ts
autonomous: true
requirements:
  - R10
must_haves:
  truths:
    - "Fighter profile API returns optional social link fields (null/absent when unset)"
    - "Admin can set/clear fighter social link fields"
    - "Public /fighters/:fighterId response includes social links only for published fighters"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Fighter optional social URLs persisted"
      contains: "model Fighter"
    - path: "backend/src/fighters/dto/fighter-profile.dto.ts"
      provides: "FighterProfileDto exposes social link fields"
      contains: "export class FighterProfileDto"
    - path: "backend/src/admin/dto/admin-fighter.dto.ts"
      provides: "Admin fighter DTO accepts social link fields"
      contains: "export class CreateAdminFighterDto"
  key_links:
    - from: "backend/src/fighters/fighters.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "prisma.fighter.findFirst select fields"
      pattern: "select: \\{[\\s\\S]*instagram|facebook|twitter|x"
    - from: "backend/src/admin/fighters.admin.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "prisma.fighter.create/update data"
      pattern: "data: \\{[\\s\\S]*instagram|facebook|twitter|x"
---

<objective>
Add optional fighter social link fields to persistence + public/admin APIs, mapped to R10.

Purpose: enable social links to be stored + served to client for conditional render/sharing.
Output: Prisma schema fields + DTOs + e2e coverage proving round-trip.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-social-sharing/14-CONTEXT.md
@.planning/phases/14-social-sharing/14-RESEARCH.md

Canonical fighter profile route (share target, used later in Plan 02):
- Frontend route: `src/app/app.routes.ts` has `explore/fighters/:fighterId` and redirect `fighters/:fighterId` -> `explore/fighters/:fighterId`.

Backend public profile endpoint:
- `GET /fighters/:fighterId` in `backend/src/fighters/fighters.controller.ts`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist social link fields on Fighter</name>
  <files>backend/prisma/schema.prisma</files>
  <behavior>
    - Add nullable string fields on `Fighter` for: Instagram, Facebook, X/Twitter.
    - Fields optional (null allowed); existing rows continue working.
  </behavior>
  <action>
    Update `model Fighter` to include three optional URL string columns.

    Field naming (choose one and use consistently across backend + frontend):
    - Preferred: `instagramUrl`, `facebookUrl`, `xUrl`
    - Acceptable alternative: `twitterUrl` instead of `xUrl` (but UI label should say "X")

    Keep type as `String?` (nullable). No arrays. No JSON.
  </action>
  <verify>
    <automated>cd backend && npm run prisma:generate</automated>
  </verify>
  <done>
    Prisma client generates cleanly with new optional Fighter fields.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose social links via public fighters profile API</name>
  <files>backend/src/fighters/fighters.service.ts, backend/src/fighters/dto/fighter-profile.dto.ts</files>
  <behavior>
    - `GET /fighters/:fighterId` returns `instagramUrl`, `facebookUrl`, `xUrl` (or twitterUrl) as `string | null`.
    - For published fighters only (existing behavior preserved).
  </behavior>
  <action>
    In `FightersService.getPublishedProfile`:
    - Extend Prisma `select` to include the three social fields.
    - Extend `FighterProfileDto` to declare the fields with `string | null`.
    - Include fields in returned object.

    Keep response backwards-compatible for clients: fields may be absent only if you intentionally omit them; prefer always present with `null` values for simplicity.
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <done>
    Public profile payload includes the three optional social link fields without breaking existing fighters listing/profile behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Allow admin create/update of fighter social links + add e2e coverage</name>
  <files>backend/src/admin/dto/admin-fighter.dto.ts, backend/src/admin/fighters.admin.service.ts, backend/test/admin.fighters.e2e-spec.ts</files>
  <behavior>
    - Admin create (`POST /admin/fighters`) accepts optional social URLs.
    - Admin update (`PATCH /admin/fighters/:fighterId`) can set/clear these fields.
    - E2E test proves: create fighter with social URLs + publish => public `/fighters/:id` returns same values.
    - Backend rejects dangerous schemes for these fields (minimum: reject `javascript:`); allowlist `https:` (and allow `http:` only if clearly required for local/dev/test).
  </behavior>
  <action>
    Extend `CreateAdminFighterDto` / `UpdateAdminFighterDto` with optional string fields for the 3 social URLs.

    Validation:
    - Prefer strict allowlist by scheme. Implement either:
      - `@IsUrl({ require_protocol: true, protocols: ['https'] })` (and optionally `http` in test/dev), OR
      - Custom validator that parses via `new URL(value)` and checks `protocol` is `https:` (and optionally `http:` for localhost).
    - Ensure `javascript:` and malformed URLs fail validation (400).

    Extend `FightersAdminService`:
    - Include social fields in `create` and `update` `data`.
    - Include social fields in `select` and returned `AdminFighterDto` (add to type).

    Add E2E test in `backend/test/admin.fighters.e2e-spec.ts`:
    - After `resetTestDb`, login admin.
    - Create fighter with `published: true` and set social URLs.
    - Fetch `GET /fighters/:fighterId` (public) and assert social URL fields match.
    - Negative case: attempt PATCH with `instagramUrl: "javascript:alert(1)"` (or similar) => expect 400.
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <done>
    Admin endpoints accept valid https URLs, reject `javascript:`/invalid URLs, and public fighter profile includes persisted values.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| Admin client → API | Untrusted input for social URL fields enters backend validation |
| Public API → Client | Social URL strings cross into rendering context (href) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-14-01 | T | `admin/fighters` DTO validation | mitigate | Reject non-allowlisted URL schemes; parse+validate URLs server-side |
| T-14-05 | I | `GET /fighters/:fighterId` payload | accept | Social URLs intended public; only served for published fighters |
| T-14-06 | D | URL validation strictness | accept | Keep validation lightweight; avoid remote fetches / DNS checks |
| T-14-04 | E | Client-side link XSS | mitigate | (Primary in Plan 02) client must sanitize before binding to `href` |
</threat_model>

<verification>
- Backend E2E suite passes (includes new social-links assertions).
- Prisma client generation passes after schema change.
</verification>

<success_criteria>
- Published fighter profile API returns optional `instagramUrl`/`facebookUrl`/`xUrl` as `string | null`.
- Admin can set/clear social URL fields and invalid schemes are rejected with 400.
</success_criteria>

<output>
After completion, create `.planning/phases/14-social-sharing/14-01-SUMMARY.md`
</output>

