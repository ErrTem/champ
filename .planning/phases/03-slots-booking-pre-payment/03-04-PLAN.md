---
phase: 03-slots-booking-pre-payment
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - backend/src/availability/availability.controller.ts
  - backend/src/bookings/bookings.controller.ts
  - src/app/pages/book-placeholder/book-placeholder.page.ts
  - src/app/pages/book-placeholder/book-placeholder.page.html
  - src/app/pages/book-placeholder/book-placeholder.page.scss
autonomous: true
requirements:
  - CAL-02
  - BKG-02
must_haves:
  truths:
    - No availability cases have friendly empty states and safe exits
    - Stale slot selection shows friendly error and refreshes the selected day without bouncing back to month view
    - Concurrency conflicts are surfaced as 409 SLOT_UNAVAILABLE and handled gracefully
  artifacts:
    - src/app/pages/book-placeholder/book-placeholder.page.ts
---

<objective>
Finish Phase 3 edge cases and polish: empty states, stale slot handling, and friendly errors that keep the user inside the picker context while safely recovering from concurrency conflicts.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-slots-booking-pre-payment/03-CONTEXT.md
@.planning/phases/03-slots-booking-pre-payment/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize stale-slot and validation errors (backend)</name>
  <files>backend/src/availability/availability.controller.ts,backend/src/bookings/bookings.controller.ts</files>
  <read_first>
d:\programming\champ-app\backend\src\availability\availability.controller.ts
d:\programming\champ-app\backend\src\bookings\bookings.controller.ts
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-CONTEXT.md
  </read_first>
  <action>
Ensure the backend returns stable, UI-actionable error shapes:

- Availability:
  - If `fighterId` or `serviceId` is invalid or the service does not belong to the fighter, return HTTP 404 or 400 with `code: "INVALID_SELECTION"` (pick one and keep it consistent).

- Create booking:
  - On reservation conflict (slot reserved/confirmed), return HTTP 409 with JSON containing:
    - `code: "SLOT_UNAVAILABLE"`
    - `message: string` (short, user-safe)

Do not leak internal DB errors in messages.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `backend/src/bookings/bookings.controller.ts` contains substring `SLOT_UNAVAILABLE`
- Availability invalid selection path includes substring `INVALID_SELECTION` (if implemented)
  </acceptance_criteria>
  <done>Backend error contracts are stable for UI-friendly handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stale slot friendly error + refresh selected day (frontend, no month bounce)</name>
  <files>src/app/pages/book-placeholder/book-placeholder.page.ts,src/app/pages/book-placeholder/book-placeholder.page.html,src/app/pages/book-placeholder/book-placeholder.page.scss</files>
  <read_first>
d:\programming\champ-app\src\app\pages\book-placeholder\book-placeholder.page.ts
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-UI-SPEC.md
  </read_first>
  <action>
Implement the D-09 stale slot handling on the Review step:

When reserve fails with 409 and `code === "SLOT_UNAVAILABLE"`:
- Show an inline tonal alert block above the CTA with exact copy from UI-SPEC:
  - Heading: “That time just got taken.”
  - Body: “Pick another time — we refreshed today’s availability.”
  - Action: “View updated times”
- Clicking “View updated times”:
  - Returns to the Select step
  - Keeps the same day selected
  - Triggers a refresh of availability and re-renders buckets for that day
  - Clears the stale selected slotId

Do not navigate back to the month-only view; keep user context on the day they were scheduling.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- Book page template contains exact string `That time just got taken.`
- Book page template contains exact string `Pick another time — we refreshed today’s availability.`
- Clicking the stale-slot action returns user to the Select step (verify manually)
  </acceptance_criteria>
  <done>Stale selection is handled with a friendly error and in-context refresh.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm empty-state coverage (no slots day + no availability 30 days)</name>
  <files>src/app/pages/book-placeholder/book-placeholder.page.html</files>
  <read_first>
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-UI-SPEC.md
  </read_first>
  <action>
Ensure both empty-state cases exist with the exact headings from UI-SPEC:

1) Selected day has zero slots:
- Heading: “No times available”
- Body: “Try another day — new sessions open up regularly.”
- Action: “Pick a different date”

2) Entire 30-day horizon has zero availability:
- Heading: “No availability in the next 30 days”
- Body: “Check back soon or choose another fighter.”
- Actions: “Back to profile”, “Explore”
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- Template contains string `No times available`
- Template contains string `No availability in the next 30 days`
- Template contains action label `Pick a different date`
  </acceptance_criteria>
  <done>Empty states are present and match the design contract copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Stale/concurrent conflicts are expected; UX must recover without confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | D | User stuck on error after race | mitigate | Provide in-context stale-slot recovery path to refreshed day view |
| T-03-11 | I | Confusing/unsafe error messaging | mitigate | Standardize error codes; show friendly copy; no raw server error strings |
</threat_model>

<verification>
- Create booking, then attempt same slot again from another tab/session:
  - Confirm UI shows the stale-slot message and returns to updated times for that same day
- Verify empty states:
  - Force no-availability responses and confirm both headings render with correct actions
</verification>

