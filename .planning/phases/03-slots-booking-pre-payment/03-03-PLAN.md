---
phase: 03-slots-booking-pre-payment
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/app/app.routes.ts
  - src/app/core/models/booking.models.ts
  - src/app/core/services/booking.service.ts
  - src/app/pages/book-placeholder/book-placeholder.page.ts
  - src/app/pages/book-placeholder/book-placeholder.page.html
  - src/app/pages/book-placeholder/book-placeholder.page.scss
  - src/app/pages/login/login.page.ts
  - src/app/pages/login/login.page.html
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - BKG-01
  - BKG-03
must_haves:
  truths:
    - `/book?fighterId=&serviceId=` is still the stable entry route and works on refresh
    - UI implements month → day → time selection with bucketed time grid and 30-day horizon
    - Reserving a slot creates a booking and shows status "Awaiting payment" immediately
    - If unauthenticated at reserve time, user is sent to /login then returned to booking flow with selection preserved
  artifacts:
    - src/app/pages/book-placeholder/book-placeholder.page.ts
    - src/app/app.routes.ts
---

<objective>
Replace the Phase 2 Book placeholder UI with the real Phase 3 booking flow in Ionic/Angular:
month calendar → pick a day → pick a time (bucketed) → review & reserve → booking created (awaiting payment).

The entry route remains `/book?fighterId=...&serviceId=...` and must work on refresh/deep-link.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@DESIGN.md
@src/design/select_date_time/code.html
@src/design/booking_summary/code.html
@.planning/REQUIREMENTS.md
@.planning/phases/03-slots-booking-pre-payment/03-CONTEXT.md
@.planning/phases/03-slots-booking-pre-payment/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed booking models + API client (availability + create booking)</name>
  <files>src/app/core/models/booking.models.ts,src/app/core/services/booking.service.ts</files>
  <read_first>
d:\programming\champ-app\src\app\core\interceptors\auth.interceptor.ts
d:\programming\champ-app\src\environments\environment.development.ts
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-CONTEXT.md
  </read_first>
  <action>
Create:

1) `src/app/core/models/booking.models.ts` with interfaces:
- `AvailabilityDay` { date: string; slots: Slot[] }
- `AvailabilityResponse` { timezone: string; range: { fromDate: string; days: number }; days: AvailabilityDay[] }
- `Slot` { slotId: string; startsAtUtc: string; endsAtUtc: string }
- `CreateBookingRequest` { slotId: string }
- `Booking` { id: string; status: string; expiresAtUtc: string; slot: { slotId: string; startsAtUtc: string; endsAtUtc: string; fighterId: string; serviceId: string } }

2) `src/app/core/services/booking.service.ts` that:
- Calls `GET /availability?fighterId=...&serviceId=...&days=30` with `withCredentials: true` (harmless for public endpoints and keeps cookie behavior consistent).
- Calls `POST /bookings` with body `{ slotId }` with `withCredentials: true`.

Error handling:
- For create booking, propagate HTTP status and the response `code` (expects `SLOT_UNAVAILABLE` on 409).
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/core/services/booking.service.ts` contains substring `/availability`
- `src/app/core/services/booking.service.ts` contains substring `/bookings`
- `src/app/core/models/booking.models.ts` contains interface `AvailabilityResponse`
  </acceptance_criteria>
  <done>Frontend has typed API client for availability + create-booking.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `/book` placeholder UI with Phase 3 flow steps (Select → Review → Created)</name>
  <files>src/app/pages/book-placeholder/book-placeholder.page.ts,src/app/pages/book-placeholder/book-placeholder.page.html,src/app/pages/book-placeholder/book-placeholder.page.scss,src/app/app.routes.ts</files>
  <read_first>
d:\programming\champ-app\src\app\app.routes.ts
d:\programming\champ-app\src\app\pages\book-placeholder\book-placeholder.page.ts
d:\programming\champ-app\src\design\select_date_time\code.html
d:\programming\champ-app\src\design\booking_summary\code.html
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-UI-SPEC.md
  </read_first>
  <action>
Implement the Phase 3 booking UI by updating the existing `BookPlaceholderPage` (keep route stable, avoid renames in Phase 3).

Route contract (locked):
- Keep `/book` in `src/app/app.routes.ts` pointing to the same page component.
- Read query params `fighterId` and `serviceId` on enter and on changes; refresh must work.

UI structure (single page with steps is acceptable):
- Step A (Select date & time):
  - Month calendar view with day cells, restricted to today..today+30 days.
  - When a day is selected, render time slots grouped into buckets:
    - Morning (< 12:00), Afternoon (12:00–17:00, optional), Evening (>= 17:00)
  - Slot tiles show states: available/selected/unavailable (unavailable can be omitted if API returns only available; still keep UI state for disabled times if needed later).
  - Footer primary CTA text: “Confirm date & time” disabled until a slot is selected.
  - Loading: skeletons for calendar and slot tiles.
  - Empty state copy must match UI-SPEC (“No times available”, “No availability in the next 30 days”).
  - Error state: tonal alert block (no borders) + Retry.

- Step B (Review & reserve):
  - Editorial title (kicker “Review”, headline “Booking”) + summary card aligned to `src/design/booking_summary/code.html` (no payment section).
  - Primary CTA: “Reserve slot”
  - On tap: show loading state “Reserving…” and call `POST /bookings` with selected `slotId`.

- Step C (Booking created):
  - Title: “Booking reserved”
  - Status chip: “Awaiting payment”
  - Summary block: fighter/service/date/time
  - Primary action: “Back to profile”
  - Secondary action: “Change time” returns to Step A (same day selected if possible)

Timezone display policy:
- Treat displayed time as America/Los_Angeles without showing label (D-04).
- Use `Intl.DateTimeFormat` with `timeZone: 'America/Los_Angeles'` when rendering slot date/time from `startsAtUtc`.

Data needs:
- Load fighter profile via existing `CatalogService` (already used in the placeholder) for names and service details.
- Load availability via `BookingService` and map slots into selected day + buckets.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `/book` route still loads `BookPlaceholderPage` (grep `app.routes.ts` for `path: 'book'` and same import)
- Book page template contains CTA copy `Confirm date & time` and `Reserve slot`
- Book page template contains heading `Booking reserved` and chip label `Awaiting payment`
- Book page rendering uses `America/Los_Angeles` (grep for this exact string)
  </acceptance_criteria>
  <done>Booking flow UI exists and matches the Phase 3 UI-SPEC contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement auth redirect return-to contract for booking reserve (preserve selection)</name>
  <files>src/app/pages/book-placeholder/book-placeholder.page.ts,src/app/pages/login/login.page.ts,src/app/pages/login/login.page.html</files>
  <read_first>
d:\programming\champ-app\src\app\pages\login\login.page.ts
d:\programming\champ-app\src\app\core\services\auth.service.ts
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-CONTEXT.md
  </read_first>
  <action>
Implement D-06 without changing global auth guard behavior:

1) When `POST /bookings` fails with 401 from the booking page:
- Navigate to `/login` and include a return url query param, e.g. `returnTo`, equal to the current `/book?...` URL PLUS any in-progress selection params (at minimum: fighterId, serviceId, selected date, and selected slotId if set).
- Do not redirect to login for availability browsing errors.

2) Update `LoginPage` so after successful login it:
- Checks `returnTo` query param; if present, `navigateByUrl(returnTo)`.
- Otherwise keep existing behavior (navigate to `/profile`).

Update the login template to preserve the existing UI; no visual changes required beyond making sure the logic works.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/pages/login/login.page.ts` reads a query param named `returnTo` (or equivalent) and calls `navigateByUrl` with it after login
- Booking page code includes navigation to `/login` on 401 from reserve
  </acceptance_criteria>
  <done>Unauthenticated reserve redirects to login and returns to booking flow with selection preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router state → Page | Query params are user-controlled input |
| Ionic app → API | Availability is public; booking creation is authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | Query param tampering (`fighterId`, `serviceId`) | mitigate | Validate presence; handle missing/invalid with friendly error + safe navigation actions |
| T-03-08 | D | UI crash on missing selection / network errors | mitigate | Robust loading/empty/error states; avoid null deref in templates |
| T-03-09 | I | Leaking auth state via availability UX | mitigate | Do not auto-redirect to login for availability; only redirect on booking reserve 401 |
</threat_model>

<verification>
- Start backend and app
- Navigate: Explore → Fighter profile → select service → `/book?...`
- Select date/time, proceed to Review, reserve:
  - When logged in: booking created screen shows “Awaiting payment”
  - When logged out: reserve triggers `/login?returnTo=...`, logging in returns to `/book?...` and selection is preserved as much as possible
</verification>

