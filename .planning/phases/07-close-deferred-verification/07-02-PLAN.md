---
phase: 07-close-deferred-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md
  - .planning/phases/03-slots-booking-pre-payment/03-VERIFICATION.md
  - backend/src/scripts/concurrency-smoke.ts
  - .planning/STATE.md
autonomous: false
requirements:
  - R1
must_haves:
  truths:
    - Phase 03 E2E booking flow validated end-to-end against running app
    - Phase 03 returnTo login behavior validated
    - Concurrency smoke yields exactly one 201 and remaining 409 SLOT_UNAVAILABLE
    - Phase 03 human_needed status resolved or documented with failures
  artifacts:
    - .planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md
    - .planning/phases/03-slots-booking-pre-payment/03-VERIFICATION.md
  key_links:
    - backend script concurrency smoke executed against real DB
user_setup:
  - Postgres running and backend connected (docker compose ok)
  - API + UI runnable locally
---

<objective>
Close Phase 03 deferred human verification: E2E booking flow, login returnTo preservation, concurrency smoke, and timezone/availability spot-checks. Update Phase 03 UAT + verification docs and milestone deferred bookkeeping.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md
@.planning/phases/03-slots-booking-pre-payment/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 03 E2E booking flow + returnTo</name>
  <files>.planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md,.planning/phases/03-slots-booking-pre-payment/03-VERIFICATION.md</files>
  <read_first>
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-HUMAN-UAT.md
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-VERIFICATION.md
d:\programming\champ-app\src\app\pages\book-placeholder\book-placeholder.page.ts
d:\programming\champ-app\src\app\pages\login\login.page.ts
  </read_first>
  <action>
With backend + UI running:

1) **E2E booking flow (logged-in)**
- Navigate to fighter profile → pick service → open `/book?fighterId=...&serviceId=...`
- Select day/time → reserve.
- Expected: booking created and UI shows "Awaiting payment".

2) **E2E login returnTo preservation**
- Log out.
- Attempt reserve again.
- Expected: redirect to `/login?returnTo=/book?...` and after login return to same booking URL with selection preserved when still valid.

Record outcomes in `.planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md` by replacing each `[pending]` with `passed` or `failed` and short notes.

If all E2E scenarios pass, update `.planning/phases/03-slots-booking-pre-payment/03-VERIFICATION.md`:
- set frontmatter `status: passed`
- remove the `human_verification` list or keep but mark tests as passed (either ok; status must be passed).
  </action>
  <verify>
    <manual>03-HUMAN-UAT.md contains pass/fail for tests 1 and 2</manual>
  </verify>
  <acceptance_criteria>
- `03-HUMAN-UAT.md` tests 1 and 2 do not contain `result: [pending]`
- If both passed, `03-VERIFICATION.md` frontmatter contains `status: passed`
  </acceptance_criteria>
  <done>E2E booking + returnTo behavior verified and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 03 concurrency smoke against live DB</name>
  <files>backend/src/scripts/concurrency-smoke.ts,.planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md</files>
  <read_first>
d:\programming\champ-app\backend\src\scripts\concurrency-smoke.ts
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-VERIFICATION.md
  </read_first>
  <action>
Run the concurrency smoke test described in `03-VERIFICATION.md`:

- Ensure backend running against real Postgres with seeded availability for at least one `slotId`.
- Execute script in a way that hits the running API (or direct DB + service, per script implementation).

Expected outcome:
- exactly 1 request returns HTTP 201
- all other parallel attempts return HTTP 409 with code `SLOT_UNAVAILABLE`

Record in `.planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md` test 3 result as `passed` or `failed` with output summary (counts of 201 vs 409, and sample response bodies).
  </action>
  <verify>
    <manual>Concurrency test result recorded; counts match expectation</manual>
  </verify>
  <acceptance_criteria>
- `03-HUMAN-UAT.md` test 3 not pending
- Notes include explicit counts `201: 1` and `409: N-1` (or equivalent)
  </acceptance_criteria>
  <done>Concurrency guard validated under real parallelism.</done>
</task>

<task type="auto">
  <name>Task 3: Availability correctness + timezone spot-check</name>
  <files>.planning/phases/03-slots-booking-pre-payment/03-HUMAN-UAT.md</files>
  <read_first>
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-VERIFICATION.md
  </read_first>
  <action>
Run Phase 03 test 4:

- Compare UI displayed times to API response timestamps for at least 2 different days including a day boundary.
- Confirm horizon is 30 days and timezone behavior matches America/Los_Angeles policy.
- Confirm reserved slots disappear until TTL expiry.

Record pass/fail in `03-HUMAN-UAT.md` test 4 with concrete notes:
- which days checked
- timezone displayed
- any mismatch examples
  </action>
  <verify>
    <manual>Timezone/availability spot-check result recorded</manual>
  </verify>
  <acceptance_criteria>
- `03-HUMAN-UAT.md` test 4 not pending
- Notes include at least one explicit timezone string (e.g. `America/Los_Angeles`)
  </acceptance_criteria>
  <done>Timezone and availability behavior validated and documented.</done>
</task>

<task type="auto">
  <name>Task 4: Update milestone deferred bookkeeping for Phase 03</name>
  <files>.planning/STATE.md</files>
  <read_first>
d:\programming\champ-app\.planning\STATE.md
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-HUMAN-UAT.md
d:\programming\champ-app\.planning\phases\03-slots-booking-pre-payment\03-VERIFICATION.md
  </read_first>
  <action>
In `.planning/STATE.md` -> `## Deferred Items` table:

- If Phase 03 human tests are all passed, update:
  - `uat_gap | Phase 03 \`03-HUMAN-UAT.md\`` status from `partial (4 pending scenarios)` to `passed (0 pending scenarios)`
  - `verification_gap | Phase 03 \`03-VERIFICATION.md\`` status from `human_needed` to `passed`

- If any fail, keep `human_needed` / `partial` but append short reason(s) in the Item cell.
  </action>
  <verify>
    <manual>STATE.md Phase 03 deferred rows match UAT + verification docs</manual>
  </verify>
  <acceptance_criteria>
- If `03-VERIFICATION.md` has `status: passed`, then STATE.md contains `Phase 03 \`03-VERIFICATION.md\` | passed`
- If `03-HUMAN-UAT.md` has no pending scenarios, STATE.md contains `Phase 03 \`03-HUMAN-UAT.md\` | passed (0 pending scenarios)`
  </acceptance_criteria>
  <done>Milestone deferred list accurate for Phase 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Booking UX correctness depends on runtime router + auth cookies |
| Parallel clients → DB | Concurrency guard correctness only proven under load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-03 | D | Slot reservation endpoint | mitigate | Concurrency smoke validates 1×201 and remaining 409; capture results |
| T-7-04 | T | Timezone boundary logic | mitigate | Spot-check around day boundary; record mismatches with evidence |
</threat_model>

<verification>
All four Phase 03 human tests in `03-HUMAN-UAT.md` are non-pending with notes; `03-VERIFICATION.md` status updated accordingly; STATE deferred items updated to match.
</verification>

