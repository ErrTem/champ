---
phase: 08-routing-app-shell-navigation
plan: 03
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-04"]
files_modified:
  - src/app/app.routes.spec.ts
  - src/app/core/guards/auth.guard.spec.ts
  - src/app/pages/login/login.page.spec.ts
  - src/app/shell/tabs.page.spec.ts
autonomous: true
requirements: [R3, R4]
user_setup: []

must_haves:
  truths:
    - "Automated tests assert R3 redirects and safe login redirect behavior."
    - "Automated tests assert R4 tab bar renders and admin tab gating."
  artifacts:
    - path: "src/app/app.routes.spec.ts"
      provides: "Router redirects tests"
      min_lines: 40
    - path: "src/app/shell/tabs.page.spec.ts"
      provides: "Tabs shell rendering + admin tab gating tests"
      min_lines: 40
  key_links:
    - from: "src/app/app.routes.spec.ts"
      to: "src/app/app.routes.ts"
      via: "imports routes config"
      pattern: "from '\\./app\\.routes'"
---

<objective>
Add automated Jasmine/Karma coverage for routing defaults, wildcard redirect, auth `returnTo` preservation + safety, and footer tabs admin gating.

Purpose: lock in R3 + R4 behavior and prevent regressions in routing/app-shell refactors.
Output: new unit/integration specs that run in `npm test`.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-routing-app-shell-navigation/08-RESEARCH.md
@.planning/phases/08-routing-app-shell-navigation/08-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add router redirect + alias specs (R3)</name>
  <read_first>
    - src/app/app.routes.ts
    - karma.conf.js
    - angular.json
  </read_first>
  <files>
    src/app/app.routes.spec.ts
  </files>
  <action>
    Create Jasmine spec covering router config invariants without requiring E2E:
    - Assert default redirect exists: `'' -> 'explore'` with `pathMatch: 'full'`.
    - Assert wildcard redirect exists: `** -> 'explore'`.
    - Assert legacy aliases exist:
      - `my-bookings` redirects to `bookings`
      - `fighters/:fighterId` redirects to `explore/fighters/:fighterId`

    Prefer reading exported `routes` array from `app.routes.ts` and asserting presence of expected route objects (shape-based assertions).
  </action>
  <acceptance_criteria>
    - Grep: `src/app/app.routes.spec.ts` contains expectations for `path: ''` redirectTo `explore`.
    - Grep: `src/app/app.routes.spec.ts` contains expectations for `path: '**'` redirectTo `explore`.
    - Grep: `src/app/app.routes.spec.ts` asserts presence of alias redirects for `my-bookings` and `fighters/:fighterId`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Router config invariants for R3 are enforced by automated test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add auth guard + login returnTo safety specs (R3)</name>
  <read_first>
    - src/app/core/guards/auth.guard.ts
    - src/app/pages/login/login.page.ts
    - src/app/core/services/auth.service.ts
  </read_first>
  <files>
    src/app/core/guards/auth.guard.spec.ts
    src/app/pages/login/login.page.spec.ts
  </files>
  <action>
    Add tests verifying:
    - `authGuard` creates UrlTree to `/login` with `queryParams.returnTo` containing attempted URL when unauth (mock Router + AuthService returning null user).
    - `LoginPage` on successful login:
      - navigates to safe `returnTo` when `returnTo` is in-app (starts with `/` and not `//`)
      - otherwise navigates to `/explore`

    Use Angular TestBed with standalone component testing patterns already used in repo (if none, implement standard Angular testing setup for standalone components).
  </action>
  <acceptance_criteria>
    - Grep: `src/app/core/guards/auth.guard.spec.ts` asserts redirect target `/login` and query param `returnTo`.
    - Grep: `src/app/pages/login/login.page.spec.ts` includes a case where `returnTo='//evil.com'` results in `/explore` navigation.
    - Grep: `src/app/pages/login/login.page.spec.ts` includes a case where `returnTo='/bookings'` results in navigation to `/bookings`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Auth flow preserves attempted route safely and defaults to `/explore` as required.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tabs shell admin gating spec (R4)</name>
  <read_first>
    - src/app/shell/tabs.page.ts
    - src/app/shell/tabs.page.html
    - src/app/core/services/auth.service.ts
  </read_first>
  <files>
    src/app/shell/tabs.page.spec.ts
  </files>
  <action>
    Add component test verifying:
    - Tabs shell renders Explore/Bookings/Profile tab buttons always.
    - Admin tab button not rendered when user is null/undefined or `isAdmin !== true`.
    - Admin tab button rendered when user has `isAdmin: true`.

    Mock AuthService profile observable to control admin/non-admin state.
  </action>
  <acceptance_criteria>
    - Grep: `src/app/shell/tabs.page.spec.ts` asserts absence of admin tab button label/icon in non-admin case.
    - Grep: `src/app/shell/tabs.page.spec.ts` asserts presence in admin case.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Automated test enforces Admin tab gating and presence of required tabs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI/test runner → routing/nav behavior | tests must detect regressions in redirects, returnTo, and admin gating |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-08-07 | T | Redirect regression (reintroduce Home / remove wildcard) | mitigate | Add explicit unit test assertions for redirect and wildcard route presence. |
| T-08-08 | S | Open redirect regression via `returnTo` | mitigate | Add explicit test rejecting `returnTo` starting with `//` and verifying fallback `/explore`. |
| T-08-09 | E | Admin UI gating regression | mitigate | Add component tests for Admin tab hidden by default and shown only for `isAdmin: true`. |
</threat_model>

<verification>
- `npm test` passes locally.
</verification>

<success_criteria>
- R3 behaviors (redirects, returnTo preservation + safety, default `/explore`) covered by automated tests.
- R4 behaviors (tabs present, admin tab gating) covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-routing-app-shell-navigation/08-03-SUMMARY.md`
</output>
