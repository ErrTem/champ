---
phase: 08-routing-app-shell-navigation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/shell/header.component.ts
  - src/app/shell/header.component.html
  - src/app/shell/header.component.scss
autonomous: true
requirements: [R3]
user_setup: []

must_haves:
  truths:
    - "All primary pages show consistent top-left back arrow."
    - "Back pops within Ionic history when present, else goes to `/explore`."
    - "Back never navigates to Home."
  artifacts:
    - path: "src/app/shell/header.component.ts"
      provides: "Shared header component with ion-back-button"
      min_lines: 30
    - path: "src/app/shell/header.component.html"
      provides: "Header template contract"
      contains: "ion-back-button"
  key_links:
    - from: "src/app/shell/header.component.html"
      to: "/explore"
      via: "ion-back-button defaultHref"
      pattern: "defaultHref=\"/explore\""
---

<objective>
Implement shared header component with explicit back arrow.

Purpose: satisfy R3 shared back navigation decision with consistent app chrome.
Output: new `HeaderComponent` to be adopted by primary pages in a separate plan.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-routing-app-shell-navigation/08-CONTEXT.md
@.planning/phases/08-routing-app-shell-navigation/08-PATTERNS.md
@.planning/phases/08-routing-app-shell-navigation/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared header component (back + title)</name>
  <read_first>
    - src/app/pages/fighter-profile/fighter-profile.page.ts
    - src/app/pages/fighter-profile/fighter-profile.page.html
    - .planning/phases/08-routing-app-shell-navigation/08-UI-SPEC.md (Header + Back Contract)
  </read_first>
  <files>
    src/app/shell/header.component.ts
    src/app/shell/header.component.html
    src/app/shell/header.component.scss
  </files>
  <action>
    Create standalone `HeaderComponent` in `src/app/shell/` using Ionic standalone imports pattern.

    Component contract:
    - Input: `title: string`
    - Input: `showBack?: boolean` default true
    - Selector MUST be `app-header` (so page templates can uniformly embed it)
    - Render `ion-header` + `ion-toolbar`
      - start slot: `ion-back-button` wrapped in `ion-buttons` when `showBack !== false`
      - `ion-back-button` MUST set `defaultHref="/explore"` and `text=""` (icon-only), per UI-SPEC
      - center: `ion-title` using `title`

    Keep styles minimal; no new colors/typography beyond Ionic defaults.
  </action>
  <acceptance_criteria>
    - Grep: `src/app/shell/header.component.html` contains `<ion-back-button defaultHref="/explore" text=""></ion-back-button>`.
    - Grep: `src/app/shell/header.component.ts` exports a standalone component with `@Component({ standalone: true, ... })` and declares `title` input.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Shared header component exists and matches UI-SPEC back behavior contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user navigation stack → back action | back behavior depends on history state and fallback |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-08-05 | T | Back fallback destination | mitigate | Hardcode `defaultHref="/explore"` in shared header component; remove any page-level divergent defaults. |
| T-08-06 | D | Broken navigation due to inconsistent headers | mitigate | Centralize header/back UI in one component and import into primary pages. |
</threat_model>

<verification>
- `npm test` passes.
- Manual smoke: navigate within Explore → fighter profile → back returns to Explore; open deep link directly then back lands on `/explore`.
</verification>

<success_criteria>
- All primary pages render consistent header chrome with explicit back arrow.
- Back arrow behavior matches UI-SPEC: pop when history exists, else deterministic `/explore` fallback (R3).
</success_criteria>

<output>
After completion, create `.planning/phases/08-routing-app-shell-navigation/08-02-SUMMARY.md`
</output>
