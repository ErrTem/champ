---
phase: 08-footer-admin-nav-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/shell/header.component.html
  - src/app/shell/header.component.ts
autonomous: true
requirements: [UI-FOOTER-01]
user_setup: []
must_haves:
  truths:
    - "No <ion-header> rendered anywhere (0 matches under src/app)."
    - "No <app-header> usage anywhere (0 matches under src/app)."
---

<objective>
Remove all header chrome everywhere by eliminating <ion-header> usage and removing <app-header> usage across app templates.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove <ion-header> and <app-header> from all templates</name>
  <read_first>
    - src/app/shell/header.component.html
  </read_first>
  <action>
    - Remove all <ion-header> blocks across src/app templates.
    - Remove all <app-header ...> usages across src/app templates.
    - Keep pages using <ion-content> as root container.
  </action>
  <acceptance_criteria>
    - rg "<ion-header" src/app returns 0 matches
    - rg "<app-header" src/app returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Clean up header component TypeScript imports</name>
  <read_first>
    - src/app/shell/header.component.ts
  </read_first>
  <action>
    Remove unused Ionic imports from header component now that template no longer uses ion-header/back.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/08-footer-admin-nav-restructure/08-01-SUMMARY.md`
</output>
