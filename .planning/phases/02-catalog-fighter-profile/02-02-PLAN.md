---
phase: 02-catalog-fighter-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/app.routes.ts
  - src/app/core/models/catalog.models.ts
  - src/app/core/services/catalog.service.ts
  - src/app/pages/catalog/catalog.page.ts
  - src/app/pages/catalog/catalog.page.html
  - src/app/pages/catalog/catalog.page.scss
  - src/app/pages/fighter-profile/fighter-profile.page.ts
  - src/app/pages/fighter-profile/fighter-profile.page.html
  - src/app/pages/fighter-profile/fighter-profile.page.scss
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - FTR-01
  - FTR-02
must_haves:
  truths:
    - Explore (catalog) loads fighters from API and renders cards with “From $X”
    - Fighter profile loads by fighterId and renders stats + services list
  artifacts:
    - src/app/pages/catalog/catalog.page.ts
    - src/app/pages/fighter-profile/fighter-profile.page.ts
---

<objective>
Implement the Ionic/Angular Explore (catalog) and Fighter Profile pages that consume the Phase 2 public APIs and match the UI contract: no 1px borders, tonal layering, skeleton loading, and robust error handling.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog-fighter-profile/02-CONTEXT.md
@.planning/phases/02-catalog-fighter-profile/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public routes for Explore and Fighter Profile (no auth guard)</name>
  <files>src/app/app.routes.ts</files>
  <read_first>
d:\programming\champ-app\src\app\app.routes.ts
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-UI-SPEC.md
  </read_first>
  <action>
Update `src/app/app.routes.ts` to add two new public routes (no `authGuard`):
- `/explore` → catalog page component
- `/fighters/:fighterId` → fighter profile page component

Ensure back navigation works with Ionic default stack, and these routes are reachable without login.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/app.routes.ts` contains a route path `explore`
- `src/app/app.routes.ts` contains a route path `fighters/:fighterId`
- Neither route includes `canActivate: [authGuard]`
  </acceptance_criteria>
  <done>Public catalog/profile routes exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Add catalog API client (types + service)</name>
  <files>src/app/core/models/catalog.models.ts,src/app/core/services/catalog.service.ts</files>
  <read_first>
d:\programming\champ-app\src\app\core\services\auth.service.ts
d:\programming\champ-app\src\environments\environment.development.ts
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-RESEARCH.md
  </read_first>
  <action>
Create:

1) `src/app/core/models/catalog.models.ts` with TypeScript interfaces matching the API contract:
- `FighterListItem` with `id`, `name`, `photoUrl?`, `summary`, `fromPriceCents: number | null`, `disciplines?: string[]`
- `FighterProfile` with `id`, `name`, `photoUrl?`, `bio`, `disciplines?: string[]`, `mediaUrls?: string[]`, stats fields (`wins`, `losses`, `draws`, `yearsPro`), and `services: Service[]`
- `Service` with `id`, `title`, `durationMinutes`, `modality`, `priceCents`, `currency`

2) `src/app/core/services/catalog.service.ts` that uses `HttpClient` to call:
- `GET /fighters` → `FighterListItem[]`
- `GET /fighters/:fighterId` → `FighterProfile`

Implementation constraints:
- Use the same base URL pattern as existing services (prefer `environment.apiUrl` and `/api` proxy).
- Public endpoints should not require auth; do not fail hard on missing session state.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/core/services/catalog.service.ts` contains substring `getFighters`
- `src/app/core/services/catalog.service.ts` contains substring `/fighters/`
- `src/app/core/models/catalog.models.ts` contains interface `FighterListItem`
  </acceptance_criteria>
  <done>Typed API client exists for Phase 2 catalog/profile endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Build Explore (catalog) page UI with skeleton/loading/error states</name>
  <files>src/app/pages/catalog/catalog.page.ts,src/app/pages/catalog/catalog.page.html,src/app/pages/catalog/catalog.page.scss</files>
  <read_first>
d:\programming\champ-app\DESIGN.md
d:\programming\champ-app\src\design\explore_fighters\code.html
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-UI-SPEC.md
  </read_first>
  <action>
Create `src/app/pages/catalog/` page following UI-SPEC:
- Header/hero section + sticky chip bar (chips static UI only; no filtering behavior yet).
- Fighters list from API:
  - Card shows image, name, summary, and “From $X” computed from `fromPriceCents` (format dollars with integer cents).
- State UX:
  - Loading uses Ionic skeletons with stable layout.
  - Empty state uses tonal surface card and exact copy from UI-SPEC.
  - Error state uses tonal alert block (no borders) with Retry action that refetches.

On tapping a fighter card, navigate to `/fighters/:fighterId`.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/pages/catalog/catalog.page.html` contains string `From`
- `src/app/pages/catalog/catalog.page.html` contains `ion-skeleton-text` (loading state)
- `src/app/pages/catalog/catalog.page.scss` does not add `border: 1px` (grep for `border:` and ensure no 1px separators)
  </acceptance_criteria>
  <done>Explore page renders catalog from API with required states and navigation.</done>
</task>

<task type="auto">
  <name>Task 4: Build Fighter Profile page UI with stats + services list</name>
  <files>src/app/pages/fighter-profile/fighter-profile.page.ts,src/app/pages/fighter-profile/fighter-profile.page.html,src/app/pages/fighter-profile/fighter-profile.page.scss</files>
  <read_first>
d:\programming\champ-app\DESIGN.md
d:\programming\champ-app\src\design\fighter_profile\code.html
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-UI-SPEC.md
  </read_first>
  <action>
Create `src/app/pages/fighter-profile/` page following UI-SPEC:
- Load fighter by route param `fighterId` via `CatalogService`.
- Render hero, bio, and **stats tiles** (wins/losses/draws/yearsPro) as required by D-05.
- Render services list with title, durationMinutes, modality, and price.
- State UX:
  - Loading skeletons
  - Error alert block + Retry
  - Empty services state copy from UI-SPEC

Do not implement booking navigation here yet beyond emitting a service selection event; Plan 03 handles the placeholder flow wiring.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/pages/fighter-profile/fighter-profile.page.html` contains `Services`
- `src/app/pages/fighter-profile/fighter-profile.page.html` references at least one stats label/value pair (e.g. wins/losses)
- `src/app/pages/fighter-profile/fighter-profile.page.scss` avoids 1px borders for containment (no `border: 1px`)
  </acceptance_criteria>
  <done>Profile page renders bio, stats, and services list from API with required states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ionic app → API | Public unauthenticated GET requests via same-origin `/api` proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | I | Accidentally requiring auth for public pages | mitigate | Ensure routes have no auth guard; API calls do not assume session |
| T-02-05 | D | UI crash on network failures | mitigate | Implement loading/empty/error states with Retry; avoid null deref in templates |
</threat_model>

<verification>
- Run the app and confirm:
  - `/explore` loads fighters and shows “From $X”
  - tapping a card navigates to `/fighters/:fighterId`
  - profile shows stats and a services list
  - offline mode shows error state with Retry
</verification>

