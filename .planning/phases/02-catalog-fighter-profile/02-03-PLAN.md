---
phase: 02-catalog-fighter-profile
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/app/app.routes.ts
  - src/app/pages/fighter-profile/fighter-profile.page.ts
  - src/app/pages/fighter-profile/fighter-profile.page.html
  - src/app/pages/book-placeholder/book-placeholder.page.ts
  - src/app/pages/book-placeholder/book-placeholder.page.html
  - src/app/pages/book-placeholder/book-placeholder.page.scss
autonomous: true
requirements:
  - FTR-03
must_haves:
  truths:
    - Selecting a service navigates immediately with fighterId + serviceId
    - Book placeholder renders deterministically on refresh with query params
  artifacts:
    - src/app/pages/book-placeholder/book-placeholder.page.ts
---

<objective>
Implement the Phase 2 “Book” entry flow: service selection is single-select and advances immediately to a placeholder route carrying `fighterId` + `serviceId`, with resilient deep-link behavior.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog-fighter-profile/02-CONTEXT.md
@.planning/phases/02-catalog-fighter-profile/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Book placeholder route contract (/book?fighterId=&amp;serviceId=)</name>
  <files>src/app/app.routes.ts</files>
  <read_first>
d:\programming\champ-app\src\app\app.routes.ts
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-UI-SPEC.md
  </read_first>
  <action>
Update `src/app/app.routes.ts` to add a new public route:
- Path: `/book`
- Component: book-placeholder page

Route params must be passed via **query params**:
- `fighterId`
- `serviceId`

This page must be reachable without auth and must render deterministically on refresh/deep-link.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- `src/app/app.routes.ts` contains a route with path `book`
  </acceptance_criteria>
  <done>Book placeholder route exists and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement D-04 service selection (single-select + immediate navigation)</name>
  <files>src/app/pages/fighter-profile/fighter-profile.page.ts,src/app/pages/fighter-profile/fighter-profile.page.html</files>
  <read_first>
d:\programming\champ-app\src\app\pages\fighter-profile\fighter-profile.page.ts
d:\programming\champ-app\src\app\pages\fighter-profile\fighter-profile.page.html
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-CONTEXT.md
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-UI-SPEC.md
  </read_first>
  <action>
Implement service selection behavior per D-04:
- Tapping a service row selects **exactly one** service (set `selectedServiceId`).
- Immediately navigate to `/book` with query params:
  - `fighterId` (current route param)
  - `serviceId` (selected service id)
- Also include a footer CTA labeled “Book session” that is hidden or disabled until a service is selected; when enabled, it navigates to the same `/book` URL.

Add a brief visual selected feedback moment (accent highlight/check) before navigation if feasible without delaying navigation noticeably.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- Fighter profile template contains a tappable element per service row that triggers navigation to `/book`
- Navigation includes both `fighterId` and `serviceId` as query params (grep for `queryParams` usage in TS)
  </acceptance_criteria>
  <done>Service selection advances to Book placeholder with correct IDs.</done>
</task>

<task type="auto">
  <name>Task 3: Build Book placeholder page (loads fighter + service summary)</name>
  <files>src/app/pages/book-placeholder/book-placeholder.page.ts,src/app/pages/book-placeholder/book-placeholder.page.html,src/app/pages/book-placeholder/book-placeholder.page.scss</files>
  <read_first>
d:\programming\champ-app\.planning\phases\02-catalog-fighter-profile\02-UI-SPEC.md
d:\programming\champ-app\DESIGN.md
  </read_first>
  <action>
Create `src/app/pages/book-placeholder/` page per UI-SPEC:
- Reads query params `fighterId` and `serviceId`.
- If missing/invalid, show tonal error state and provide “Back to Explore”.
- Otherwise load fighter profile via existing `CatalogService` using `fighterId`, find the selected service by `serviceId`, and render a summary card with fighter name + service title/duration/modality/price.
- Show the informational message: “Scheduling will be available in the next phase. You’ve selected this service successfully.”
- Primary CTA “Choose a time” should be disabled or replaced with “Coming soon” (must not imply a booking is created).
- Secondary action “Back to profile” navigates to `/fighters/:fighterId`.

Implement loading skeletons and error states without 1px borders.
  </action>
  <verify>
    <automated>npm test -- --watch=false</automated>
  </verify>
  <acceptance_criteria>
- Book placeholder HTML contains string `Scheduling will be available in the next phase`
- Book placeholder TS reads both `fighterId` and `serviceId` from query params
- Missing params path renders a “Back to Explore” action
  </acceptance_criteria>
  <done>Book placeholder confirms selection and supports refresh/deep-link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router state → Page | Query params are user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | Query param tampering (`fighterId`, `serviceId`) | mitigate | Validate presence, handle 404/errors gracefully, never assume service exists |
| T-02-07 | D | Refresh/deep-link instability | mitigate | Drive state from URL query params and API fetch; do not rely on in-memory selection only |
</threat_model>

<verification>
- Navigate explore → profile → select service → book placeholder.
- Refresh on `/book?fighterId=...&serviceId=...` and confirm the page still renders the same selection.
- Remove a query param and confirm the error state appears with Back to Explore action.
</verification>

