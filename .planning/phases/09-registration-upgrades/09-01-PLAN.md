---
phase: 09-registration-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/auth.service.ts
  - backend/src/auth/dto/register.dto.ts
  - backend/src/users/users.service.ts
  - backend/src/users/users.controller.ts
  - backend/src/admin/fighter-approvals.admin.controller.ts
  - backend/src/admin/fighter-approvals.admin.service.ts
  - backend/src/admin/admin.module.ts
autonomous: true
requirements: [R5]
user_setup: []

must_haves:
  truths:
    - "Server rejects registration missing phone, 18+ confirmation, terms acceptance, or profile type."
    - "`/users/me` returns fighter status fields so UI can render pending/approved."
    - "Fighter registration defaults to pending until admin approval."
    - "Admin-only endpoint exists to list pending fighters and approve them."
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: "User fields for type + approval and OAuth identity table"
    - path: backend/src/admin/fighter-approvals.admin.controller.ts
      provides: "Approve fighter accounts API"
    - path: backend/src/users/users.controller.ts
      provides: "`/users/me` includes new fields"
---

<objective>
Add backend support for registration upgrades: required phone + terms/18+ + profile type, plus fighter pending/approval workflow exposed via `/users/me` and admin endpoints.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-registration-upgrades/09-CONTEXT.md
@.planning/phases/09-registration-upgrades/09-RESEARCH.md
@backend/prisma/schema.prisma
@backend/src/auth/auth.controller.ts
@backend/src/auth/auth.service.ts
@backend/src/auth/dto/register.dto.ts
@backend/src/users/users.controller.ts
@backend/src/users/users.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend User model for profile type + fighter approval</name>
  <files>backend/prisma/schema.prisma</files>
  <action>
Add fields to `User`:

- `userType` (string or enum): `user|fighter` (default `user`)
- `fighterStatus` (string or enum): `none|pending|approved|rejected` (default `none`)
- `fighterApprovedAt` DateTime? (optional)
- `acceptedTermsAt` DateTime? (required for new registrations; stored timestamp)
- `confirmedAdultAt` DateTime? (required for new registrations; stored timestamp)

Run Prisma migration. Ensure existing rows get defaults and continue to work.
  </action>
  <acceptance_criteria>
    - Prisma migrate applies cleanly.
    - Existing login/refresh flows still work.
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
  </verify>
  <done>
    User schema supports `userType` + fighter approval + terms/adult timestamps with backwards-compatible defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce new registration constraints server-side</name>
  <files>backend/src/auth/dto/register.dto.ts,backend/src/auth/auth.service.ts</files>
  <action>
Update `RegisterDto` to include:

- `phone` required and validated (minimal: non-empty string; preferred: E.164-like or US mask normalized)
- `acceptedTerms` boolean required true
- `confirmedAdult` boolean required true
- `profileType` enum/string required: `user|fighter`

In `AuthService.register`, persist:
- `phone` normalized
- `userType` and `fighterStatus` (`pending` when fighter else `none`)
- `acceptedTermsAt` and `confirmedAdultAt` timestamps (now)

Return safe user DTO including new fields (no passwordHash).
  </action>
  <acceptance_criteria>
    - POST `/auth/register` rejects missing/invalid fields with 400.
    - Fighter registration creates user with `fighterStatus=pending`.
    - No endpoint allows a user to self-upgrade from `user` to `fighter` outside registration (server-side enforcement by omission).
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
  </verify>
  <done>
    Backend registration persists required fields and sets fighter pending status deterministically.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expose new user fields in `/users/me`</name>
  <files>backend/src/users/users.service.ts</files>
  <action>
Ensure `findSafeById` returns safe DTO including:
- `userType`
- `fighterStatus`
- `fighterApprovedAt`
- `acceptedTermsAt` and `confirmedAdultAt` (optional to expose; if sensitive, expose booleans instead)

Maintain existing fields: `id,email,name,phone,isAdmin`.
  </action>
  <acceptance_criteria>
    - `/users/me` response includes `userType` + `fighterStatus` (and `fighterApprovedAt` when approved).
    - Frontend AuthService `loadProfile()` continues to parse `/users/me` once types updated (Plan 09-02).
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
  </verify>
  <done>
    `/users/me` safe DTO includes fighter type/status so frontend can render pending/approved messaging.
  </done>
</task>

<task type="auto">
  <name>Task 4: Admin endpoints to approve fighter accounts</name>
  <files>backend/src/admin/fighter-approvals.admin.controller.ts,backend/src/admin/fighter-approvals.admin.service.ts,backend/src/admin/admin.module.ts</files>
  <action>
Add admin-only endpoints:

- `GET /admin/fighter-approvals?status=pending` returns pending fighter users
- `POST /admin/fighter-approvals/:userId/approve` sets `fighterStatus=approved`, `fighterApprovedAt=now`
- (optional) reject endpoint

Protect with existing `AdminGuard` + `JwtAccessAuthGuard`.
  </action>
  <acceptance_criteria>
    - Non-admin receives 403.
    - Admin can list + approve.
  </acceptance_criteria>
  <verify>
    <automated>backend: npm test</automated>
  </verify>
  <done>
    Admin can approve pending fighter users; non-admin cannot access these endpoints.
  </done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| Client bypasses required checkboxes | Tampering | Validate booleans server-side; store timestamps. |
| Non-admin approves fighters | Elevation | Guard admin endpoints with DB-checked AdminGuard. |
</threat_model>

<verification>
- `backend` test suite green.
- Manual smoke: register fighter → pending; admin approves; `/users/me` shows approved.
</verification>
