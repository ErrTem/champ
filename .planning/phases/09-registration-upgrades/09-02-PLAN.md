---
phase: 09-registration-upgrades
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/app/pages/register/register.page.ts
  - src/app/pages/register/register.page.html
  - src/app/pages/register/register.page.scss
  - src/app/core/services/auth.service.ts
  - src/app/app.routes.ts
  - src/app/pages/terms/terms.page.ts (new)
  - src/app/pages/terms/terms.page.html (new)
  - src/app/pages/terms/terms.page.scss (new)
autonomous: true
requirements: [R5]
user_setup: []

must_haves:
  truths:
    - "Register submit disabled/blocked until phone valid + both checkboxes checked + profile type chosen."
    - "Terms route exists and linked from register page."
  artifacts:
    - path: src/app/pages/register/register.page.html
      provides: "Registration form with required fields + checkboxes + profile type"
    - path: src/app/pages/terms/terms.page.html
      provides: "Mock terms content"
---

<objective>
Upgrade frontend registration UX per R5: phone mask, required checkboxes, profile type, Terms page, and pending fighter messaging wired to backend fields.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-registration-upgrades/09-CONTEXT.md
@src/app/pages/register/register.page.ts
@src/app/pages/register/register.page.html
@src/app/core/services/auth.service.ts
@src/app/app.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AuthUser shape to include fighter fields</name>
  <files>src/app/core/services/auth.service.ts</files>
  <action>
Extend `AuthUser` type to include:
- `userType`
- `fighterStatus`
- `fighterApprovedAt` (optional)

Ensure `loadProfile()` still works and consumers compile.
  </action>
  <verify>
    <automated>frontend: npm test</automated>
  </verify>
  <done>
    Frontend user model matches `/users/me` fighter fields and compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registration form fields + client-side gating</name>
  <files>src/app/pages/register/register.page.ts,src/app/pages/register/register.page.html,src/app/pages/register/register.page.scss</files>
  <action>
Update Register page:
- Phone input:
  - UI shows USA prefix + input mask (minimum: format as `(XXX) XXX-XXXX` while typing)
  - Store raw digits; send normalized phone to backend (E.164 `+1...` preferred)
- Add required checkbox: 18+ confirmation
- Add required checkbox: accept terms, with link to `/terms`
- Add profile type selector: `Regular` vs `Fighter`
- Submit button disabled until all required fields satisfied; still show server error on submit failure.

Update `auth.register(...)` payload to include required fields expected by backend Plan 09-01.
  </action>
  <acceptance_criteria>
    - Cannot submit without phone + both checkboxes + profile type.
    - Errors render inline when server rejects.
  </acceptance_criteria>
  <verify>
    <automated>frontend: npm test</automated>
  </verify>
  <done>
    Register page enforces required inputs client-side and sends payload matching backend contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Terms page route</name>
  <files>src/app/app.routes.ts,src/app/pages/terms/*</files>
  <action>
Add new route `/terms` rendering a static Terms page with minimal content.
Ensure Register page links to it.
  </action>
  <verify>
    <automated>frontend: npm test</automated>
  </verify>
  <done>
    Terms page exists at `/terms` and registration links to it.
  </done>
</task>

</tasks>

<verification>
- Manual smoke: registration gating works; terms route loads; fighter pending banner shows.
</verification>
