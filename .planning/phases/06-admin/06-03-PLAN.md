---
phase: 06-admin
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - backend/src/admin/admin.module.ts
  - backend/src/admin/fighters.admin.controller.ts
  - backend/src/admin/fighters.admin.service.ts
  - backend/src/admin/services.admin.controller.ts
  - backend/src/admin/services.admin.service.ts
  - backend/src/admin/dto/admin-fighter.dto.ts
  - backend/src/admin/dto/admin-service.dto.ts
  - backend/test/admin.authz.e2e-spec.ts
  - backend/test/admin.fighters.e2e-spec.ts
  - backend/test/admin.services.e2e-spec.ts
  - backend/test/helpers/admin-login.ts
autonomous: true
requirements:
  - ADM-02
  - ADM-03
must_haves:
  truths:
    - "Admin can create/update/deactivate fighters; public catalog reflects published fighters immediately."
    - "Admin can create/update/deactivate services/prices for a fighter; public fighter profile reflects published services immediately."
  artifacts:
    - path: "backend/src/admin/fighters.admin.controller.ts"
      provides: "Admin-only fighter CRUD endpoints (guarded)"
    - path: "backend/src/admin/services.admin.controller.ts"
      provides: "Admin-only service CRUD endpoints (guarded)"
    - path: "backend/test/admin.fighters.e2e-spec.ts"
      provides: "ADM-02 e2e coverage"
    - path: "backend/test/admin.services.e2e-spec.ts"
      provides: "ADM-03 e2e coverage"
---

<objective>
Implement admin CRUD APIs for fighters and services/prices and prove via e2e tests that public catalog/profile APIs update immediately.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-admin/06-CONTEXT.md
@.planning/phases/06-admin/06-RESEARCH.md
@.planning/phases/06-admin/06-PATTERNS.md

Constraints:
- All admin endpoints use `JwtAccessAuthGuard` + `AdminGuard` (from Plan 06-02).
- Use existing `published` booleans on `Fighter` and `Service` so changes reflect immediately in public endpoints.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin fighters CRUD endpoints (writes Fighter.published)</name>
  <files>
  - backend/src/admin/fighters.admin.controller.ts
  - backend/src/admin/fighters.admin.service.ts
  - backend/src/admin/dto/admin-fighter.dto.ts
  - backend/src/admin/admin.module.ts
  </files>
  <read_first>
  - backend/src/fighters/fighters.controller.ts + fighters.service.ts (public read behavior + select shapes)
  - backend/src/auth/guards/admin.guard.ts
  - backend/src/admin/admin.module.ts
  </read_first>
  <action>
  - Add guarded endpoints:
    - `GET /admin/fighters` returns all fighters (published + unpublished), ordered by name asc.
    - `POST /admin/fighters` creates fighter with fields: `name` (required), `bio?`, `photoUrl?`, `disciplines?`, `summary?`, `published?` (default false).
    - `PATCH /admin/fighters/:id` updates same fields including `published` toggle.
  - DTO validation: name non-empty; optional strings max length reasonable.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin.fighters`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Admin can create and edit fighters via `/admin/fighters`.
  - Public `GET /fighters` includes fighter only when `published=true` (immediate).
  - Non-admin gets 403 for all `/admin/fighters*` endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin services/prices CRUD endpoints (writes Service.published + price fields)</name>
  <files>
  - backend/src/admin/services.admin.controller.ts
  - backend/src/admin/services.admin.service.ts
  - backend/src/admin/dto/admin-service.dto.ts
  - backend/src/admin/admin.module.ts
  </files>
  <read_first>
  - backend/src/fighters/fighters.service.ts (public profile filters services by `published`)
  - backend/prisma/schema.prisma (Service fields)
  </read_first>
  <action>
  - Implement nested endpoints:
    - `GET /admin/fighters/:fighterId/services` lists services for fighter (all published states).
    - `POST /admin/fighters/:fighterId/services` creates service with fields:
      `title`, `durationMinutes`, `modality`, `priceCents`, `currency` (default), `published` (default false).
    - `PATCH /admin/services/:serviceId` updates same fields including `published` toggle.
  - Guard all routes with `JwtAccessAuthGuard` + `AdminGuard`.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin.services`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Admin can create and edit services and prices.
  - Public `GET /fighters/:id` includes service only when `published=true` (immediate) and reflects updated priceCents.
  - Non-admin gets 403 for all `/admin/*services*` endpoints.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2E tests for admin authz + fighters/services reflection</name>
  <files>
  - backend/test/helpers/admin-login.ts
  - backend/test/admin.authz.e2e-spec.ts
  - backend/test/admin.fighters.e2e-spec.ts
  - backend/test/admin.services.e2e-spec.ts
  </files>
  <read_first>
  - backend/test/helpers/test-app.ts
  - backend/test/bookings.my-bookings.e2e-spec.ts
  - backend/test/helpers/test-db.ts
  </read_first>
  <action>
  - Add helper `backend/test/helpers/admin-login.ts`:
    - Create admin user (`isAdmin=true`) + normal user (`isAdmin=false`) with known passwords.
    - Log in through real `/auth/login`, return authenticated supertest agents.
  - Add `admin.authz.e2e-spec.ts`: non-admin 403 on `/admin/ping` and `/admin/fighters`; admin 200.
  - Add `admin.fighters.e2e-spec.ts`: create fighter published false → not in public; toggle true → in public.
  - Add `admin.services.e2e-spec.ts`: create service published false → not in public profile; toggle true → in public; update priceCents → visible in public.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin.authz`
  - `cd backend && npm test -- admin.fighters`
  - `cd backend && npm test -- admin.services`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Focused admin e2e tests exist and pass.
  - Tests reset DB deterministically and do not require external services.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| Admin client → Admin CRUD APIs | Must not allow non-admin writes. |
| Admin APIs → Public APIs | Public endpoints must never expose unpublished entities. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-06-05 | E (Elevation) | `/admin/fighters*`, `/admin/*services*` | mitigate | Guard all handlers with `JwtAccessAuthGuard, AdminGuard`; e2e asserts 403 for non-admin. |
| T-06-06 | T (Tampering) | Publish state toggles | mitigate | Only admin can set `published`; public reads always filter `published=true`. |
| T-06-07 | I (Information disclosure) | Admin list responses | accept | Admin endpoints may return unpublished entities; access limited to admin only. |
</threat_model>

<verification>
<automated>
- `cd backend && npm test -- admin.authz`
- `cd backend && npm test -- admin.fighters`
- `cd backend && npm test -- admin.services`
- `cd backend && npm test`
</automated>
</verification>

<success_criteria>
- Admin CRUD endpoints exist and are admin-guarded.
- Public catalog/profile reflects published changes immediately (covered by e2e).
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin/06-03-SUMMARY.md`
</output>

