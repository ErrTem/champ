---
phase: 06-admin
plan: "06"
type: execute
wave: 6
depends_on:
  - 06-03
files_modified:
  - src/app/pages/admin/admin-fighters.page.ts
  - src/app/pages/admin/admin-fighters.page.html
  - src/app/pages/admin/admin-services.page.ts
  - src/app/pages/admin/admin-services.page.html
  - src/app/core/services/admin-api.service.ts
autonomous: true
requirements:
  - ADM-02
  - ADM-03
must_haves:
  truths:
    - "Admin Fighters tab supports list + create/edit + published toggle wired to /admin/fighters APIs."
    - "Admin Services/Prices tab supports fighter selection + service CRUD + published toggle wired to admin service APIs."
  artifacts:
    - path: "src/app/pages/admin/admin-fighters.page.ts"
      provides: "Fighters CRUD UI wired to admin fighters endpoints"
    - path: "src/app/pages/admin/admin-services.page.ts"
      provides: "Services/prices CRUD UI wired to admin services endpoints"
---

<objective>
Wire Fighters and Services/Prices admin tabs to backend admin CRUD APIs so staff can manage catalog without code changes (ADM-02, ADM-03).
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin/06-UI-SPEC.md
@.planning/phases/06-admin/06-CONTEXT.md
@.planning/phases/06-admin/06-PATTERNS.md

Plan dependencies:
- Admin shell + routes exist (Plan 06-02)
- Admin fighters/services APIs exist + tested (Plan 06-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fighters tab UI (list + create/edit + published toggle)</name>
  <files>
  - src/app/pages/admin/admin-fighters.page.ts
  - src/app/pages/admin/admin-fighters.page.html
  - src/app/core/services/admin-api.service.ts
  </files>
  <read_first>
  - src/app/pages/admin/admin-fighters.page.* (placeholder from Plan 06-02)
  - src/app/pages/my-bookings/* (loading/error/empty UI patterns)
  - .planning/phases/06-admin/06-UI-SPEC.md (Fighters contract)
  </read_first>
  <action>
  - Create `AdminApiService` (or equivalent) to wrap admin endpoints with `withCredentials: true`:
    - `listFighters()` -> `GET /admin/fighters`
    - `createFighter(payload)` -> `POST /admin/fighters`
    - `updateFighter(id, patch)` -> `PATCH /admin/fighters/:id`
  - Implement Fighters UI:
    - List fighters ordered by name; show published state chip/toggle.
    - Create/edit form inline (simple modal or inline card ok): name required, bio/photoUrl/summary optional, published toggle.
    - Save uses primary CTA styling (`.kinetic-gradient`) per design system.
    - State handling: skeleton rows while loading; inline error card + Retry; empty state “Nothing here yet”.
    - 403 handling: show toast “Admin access required” and route away (keep consistent with adminGuard UX).
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Admin can create and edit fighters and toggle published.
  - Changing published immediately changes public catalog behavior (already enforced by backend; UI surfaces toggle reliably).
  - UI matches UI-SPEC state requirements (skeleton/error/empty; no-line rule).
  </done>
</task>

<task type="auto">
  <name>Task 2: Services/Prices tab UI (fighter selector + service CRUD + published toggle)</name>
  <files>
  - src/app/pages/admin/admin-services.page.ts
  - src/app/pages/admin/admin-services.page.html
  - src/app/core/services/admin-api.service.ts
  </files>
  <read_first>
  - src/app/pages/admin/admin-services.page.* (placeholder from Plan 06-02)
  - .planning/phases/06-admin/06-UI-SPEC.md (Services contract)
  - backend/prisma/schema.prisma (service fields: duration/modality/priceCents/currency/published)
  </read_first>
  <action>
  - Extend `AdminApiService`:
    - `listFighterServices(fighterId)` -> `GET /admin/fighters/:fighterId/services`
    - `createFighterService(fighterId, payload)` -> `POST /admin/fighters/:fighterId/services`
    - `updateService(serviceId, patch)` -> `PATCH /admin/services/:serviceId`
  - Implement Services/Prices UI:
    - Require fighter selection first (top select).
    - List services for selected fighter with fields: title, durationMinutes, modality, priceCents (+ currency), published.
    - Create/edit form: inputs for title/duration/modality/price, published toggle.
    - State handling: skeleton/error/empty; retry button; keep tonal layering.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Admin can manage services/prices for selected fighter and toggle published.
  - UI uses fighter selector and does not expose services CRUD without selected fighter.
  - UI uses design contract (no divider lines; spacing tokens; gradient primary CTA for save).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| Admin UI → Admin CRUD APIs | Must handle 403 + errors; no reliance on client-only gating. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-06-15 | T (Tampering) | Published toggles | mitigate | UI writes only through guarded admin APIs; backend remains source of truth. |
| T-06-16 | I (Information disclosure) | Unpublished entities in UI | accept | Admin UI shows unpublished by design; restricted to adminGuard. |
</threat_model>

<verification>
<automated>
- `cd backend && npm test -- admin`
- `cd backend && npm test`
</automated>
</verification>

<success_criteria>
- Admin can manage fighters and services/prices end-to-end via UI without code changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin/06-06-SUMMARY.md`
</output>

