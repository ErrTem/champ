---
phase: 06-admin
plan: "05"
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - src/app/pages/admin/admin-schedule.page.ts
  - src/app/pages/admin/admin-schedule.page.html
  - src/app/pages/admin/admin-services.page.ts
  - src/app/pages/admin/admin-services.page.html
  - src/app/pages/admin/admin-bookings.page.ts
  - src/app/pages/admin/admin-bookings.page.html
  - src/app/pages/admin/admin-booking-detail.page.ts
  - src/app/pages/admin/admin-booking-detail.page.html
autonomous: true
requirements:
  - ADM-04
  - ADM-05
must_haves:
  truths:
    - "Admin Schedule tab supports weekly rules editing in PT and saving via admin API."
    - "Admin Bookings tab supports required filters and read-only booking detail (no actions)."
  artifacts:
    - path: "src/app/pages/admin/admin-schedule.page.ts"
      provides: "Schedule editor UI per UI-SPEC"
    - path: "src/app/pages/admin/admin-bookings.page.ts"
      provides: "Bookings list UI + filters per D-12"
    - path: "src/app/pages/admin/admin-booking-detail.page.ts"
      provides: "Read-only booking detail UI per D-13"
---

<objective>
Implement Ionic admin UI wiring for Schedule + Bookings tabs (filters + read-only detail), consistent with UI-SPEC and PT timezone semantics.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin/06-UI-SPEC.md
@.planning/phases/06-admin/06-CONTEXT.md
@.planning/phases/06-admin/06-PATTERNS.md

Locked decisions:
- D-10 show/input times in America/Los_Angeles (PT)
- D-12 bookings list filters: status, date range, fighter
- D-13 read-only bookings (no cancel/mutation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule tab UI (weekly rules editor in PT)</name>
  <files>
  - src/app/pages/admin/admin-schedule.page.ts
  - src/app/pages/admin/admin-schedule.page.html
  </files>
  <read_first>
  - src/app/pages/admin/admin-schedule.page.* (placeholder from Plan 06-02)
  - src/app/pages/my-bookings/* (skeleton/error/empty patterns)
  - .planning/phases/06-admin/06-UI-SPEC.md (Schedule contract)
  </read_first>
  <action>
  - Add fighter selector (required) and fetch schedule rules via admin API.
  - Render 7-day list with 0..N windows per day; allow add/remove window.
  - Inputs: start/end times; show label `Times shown in PT`.
  - Save calls `PUT /admin/fighters/:fighterId/schedule-rules`; show success toast; handle loading/skeleton/error states.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Admin can load and save weekly rules from Schedule tab.
  - UI uses PT label and no-line/tonal layering rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bookings list tab UI (filters + list rows)</name>
  <files>
  - src/app/pages/admin/admin-bookings.page.ts
  - src/app/pages/admin/admin-bookings.page.html
  </files>
  <read_first>
  - src/app/pages/admin/admin-bookings.page.* (placeholder from Plan 06-02)
  - src/app/pages/my-bookings/* (list row + filter UI patterns)
  - .planning/phases/06-admin/06-UI-SPEC.md (Bookings contract)
  </read_first>
  <action>
  - Add required filters:
    - status select
    - date range (from/to)
    - fighter select
  - Fetch bookings via admin API and render list rows (fighter, service, time, status).
  - Loading: skeleton rows; error: inline card + Retry; empty: “Nothing here yet”.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Bookings list renders with all required filters and stable refresh behavior.
  - No mutation actions present in list UI.
  </done>
</task>

<task type="auto">
  <name>Task 3: Booking detail page (read-only)</name>
  <files>
  - src/app/pages/admin/admin-booking-detail.page.ts
  - src/app/pages/admin/admin-booking-detail.page.html
  </files>
  <read_first>
  - src/app/pages/booking-detail/* (read-only detail patterns)
  - .planning/phases/06-admin/06-UI-SPEC.md
  </read_first>
  <action>
  - Implement route from bookings list row → booking detail page.
  - Fetch detail from `GET /admin/bookings/:id` (Plan 06-04) and display fields:
    fighter, service, time, user minimal identity, booking status, payment status, price.
  - No actions/buttons that mutate booking (D-13).
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Booking detail page displays required fields and has no mutation actions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| Admin UI → Admin API | UI must not assume success; handle 403 and errors gracefully. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-06-13 | I (Information disclosure) | Admin bookings UI | mitigate | Only accessible behind adminGuard; do not display more PII than API provides. |
| T-06-14 | T (Tampering) | UI adds booking actions | mitigate | Ensure no cancel/mutation actions exist in admin booking UI (D-13). |
</threat_model>

<verification>
<automated>
- `cd backend && npm test -- admin`
- `cd backend && npm test`
</automated>
</verification>

<success_criteria>
- Schedule tab edits weekly rules in PT and saves successfully.
- Bookings tab supports filters and read-only detail with no mutation actions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin/06-05-SUMMARY.md`
</output>

