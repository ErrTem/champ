---
phase: 06-admin
plan: "04"
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - backend/src/admin/schedule.admin.controller.ts
  - backend/src/admin/schedule.admin.service.ts
  - backend/src/admin/bookings.admin.controller.ts
  - backend/src/admin/bookings.admin.service.ts
  - backend/src/admin/dto/admin-schedule.dto.ts
  - backend/src/admin/dto/admin-bookings.dto.ts
  - backend/test/admin.schedule.e2e-spec.ts
  - backend/test/admin.bookings.e2e-spec.ts
autonomous: true
requirements:
  - ADM-04
  - ADM-05
must_haves:
  truths:
    - "Admin can edit weekly schedule rules (PT), save, and system regenerates rolling next 30 days of slots."
    - "Schedule edits never modify/delete slots with confirmed bookings; reserved slots protected while hold active."
    - "Admin can query bookings across all fighters with filters (status, date range, fighter) read-only."
  artifacts:
    - path: "backend/src/admin/schedule.admin.service.ts"
      provides: "Schedule rules replace + safe slot regeneration per D-07..D-10"
    - path: "backend/src/admin/bookings.admin.service.ts"
      provides: "Admin bookings list query with required filters per D-11..D-13"
    - path: "backend/test/admin.schedule.e2e-spec.ts"
      provides: "ADM-04 regen safety coverage"
    - path: "backend/test/admin.bookings.e2e-spec.ts"
      provides: "ADM-05 filters + determinism coverage"
---

<objective>
Implement backend admin schedule management (weekly rules) with safe rolling 30-day slot regeneration, and backend admin bookings visibility (read-only list/detail) with required filters. Prove with e2e tests.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-admin/06-CONTEXT.md
@.planning/phases/06-admin/06-RESEARCH.md
@.planning/phases/06-admin/06-PATTERNS.md

Locked decisions:
- D-07 weekly schedule rules edit (FighterScheduleRule)
- D-08 regen rolling 30 days
- D-09 never modify confirmed booking slots
- D-10 times input/display America/Los_Angeles
- D-11 bookings across all fighters
- D-12 filters status/date range/fighter
- D-13 read-only (no mutations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule rules replace + safe slot regeneration (per D-07..D-10)</name>
  <files>
  - backend/src/admin/schedule.admin.controller.ts
  - backend/src/admin/schedule.admin.service.ts
  - backend/src/admin/dto/admin-schedule.dto.ts
  </files>
  <read_first>
  - backend/src/availability/availability.service.ts (slot generation + timezone constant + safe slot filters)
  - backend/prisma/schema.prisma (FighterScheduleRule + Slot fields: reservedUntilUtc/reservedBookingId/confirmedBookingId)
  - backend/prisma/seed.ts (replace-all schedule rule pattern)
  </read_first>
  <action>
  - Implement guarded admin schedule rules API:
    - `GET /admin/fighters/:fighterId/schedule-rules` returns rule set sorted by dayOfWeek then startMinute.
    - `PUT /admin/fighters/:fighterId/schedule-rules` replaces rules with array of:
      `{ dayOfWeek: 0..6, startMinute: 0..1439, endMinute: 1..1440, active: boolean }`.
    - Validate day range + start<end; reject overlaps per day.
  - After PUT, run regeneration for rolling 30 days (D-08) using America/Los_Angeles as local basis (D-10):
    - Compute `startLocal = now PT startOf('day')`, `endLocalExclusive = startLocal.plus({ days: 30 })`.
    - Reuse availability generation logic per service duration.
    - Create missing slots via `createMany({ skipDuplicates: true })`.
    - Delete obsolete slots only when SAFE:
      - `confirmedBookingId == null`
      - AND (`reservedUntilUtc == null` OR `reservedUntilUtc < nowUtc`)
    - Never update existing slots; only create/delete safe.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin.schedule`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Schedule update triggers regen; confirmed slots never deleted/altered.
  - Active holds (reservedUntilUtc >= nowUtc) not deleted during regen.
  - PT policy used for bounds and rule interpretation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin bookings list + detail endpoints (filters, deterministic ordering, read-only) (per D-11..D-13)</name>
  <files>
  - backend/src/admin/bookings.admin.controller.ts
  - backend/src/admin/bookings.admin.service.ts
  - backend/src/admin/dto/admin-bookings.dto.ts
  </files>
  <read_first>
  - backend/src/bookings/bookings.service.ts (ordering + DTO patterns)
  - backend/src/payments/payments.service.ts (booking/payment status fields used)
  - backend/prisma/schema.prisma (Booking, Slot, Fighter, Service relations)
  </read_first>
  <action>
  - Implement guarded admin bookings endpoints:
    - `GET /admin/bookings` supports filters:
      - `status`
      - `fighterId` (optional)
      - `from` and `to` as `YYYY-MM-DD` interpreted as PT day bounds and converted to UTC.
    - Deterministic ordering: `slot.startsAtUtc asc`, then `booking.id asc`.
    - Return minimal PII: user id + name/email only.
    - Optional `GET /admin/bookings/:id` read-only detail.
  - No mutation endpoints (D-13).
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin.bookings`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - Filters return correct subsets and order stable.
  - Non-admin gets 403; admin can read all bookings.
  - No booking mutation endpoints added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2E tests for regen safety + bookings filters</name>
  <files>
  - backend/test/admin.schedule.e2e-spec.ts
  - backend/test/admin.bookings.e2e-spec.ts
  </files>
  <read_first>
  - backend/test/helpers/admin-login.ts (from Plan 06-03)
  - backend/test/helpers/test-app.ts
  - backend/test/bookings.my-bookings.e2e-spec.ts (cookie helper patterns)
  </read_first>
  <action>
  - Add `admin.schedule.e2e-spec.ts`:
    - Create confirmed slot in horizon, change rules removing it, regen, assert slot still exists.
    - Create active hold slot (reservedUntilUtc future), change rules, regen, assert slot still exists.
  - Add `admin.bookings.e2e-spec.ts`:
    - Create bookings across fighters/statuses/dates; assert filters and deterministic ordering.
  </action>
  <verify>
  <automated>
  - `cd backend && npm test -- admin.schedule`
  - `cd backend && npm test -- admin.bookings`
  - `cd backend && npm test`
  </automated>
  </verify>
  <done>
  - `cd backend && npm test -- admin.schedule` passes.
  - `cd backend && npm test -- admin.bookings` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| Schedule write → slots | Regen can delete data; must protect confirmed + active holds. |
| Bookings list → PII | Must remain admin-only and minimal. |
| Date inputs → query bounds | PT vs UTC conversion correctness prevents wrong exposure. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-06-09 | T (Tampering) | Slot regen deletion | mitigate | Delete only when confirmedBookingId null + no active hold; e2e asserts confirmed + reserved protected. |
| T-06-11 | I (Information disclosure) | Admin bookings list | mitigate | Guard via AdminGuard; return minimal PII. |
| T-06-12 | T (Tampering) | Date range filters | mitigate | Interpret dates as PT day bounds and convert to UTC; e2e coverage. |
</threat_model>

<verification>
<automated>
- `cd backend && npm test -- admin.schedule`
- `cd backend && npm test -- admin.bookings`
- `cd backend && npm test`
</automated>
</verification>

<success_criteria>
- Admin can update schedule rules and regen slots safely for next 30 days.
- Admin can view bookings with required filters and deterministic ordering (read-only).
- e2e coverage exists for ADM-04 and ADM-05 behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin/06-04-SUMMARY.md`
</output>

