---
phase: 04-payments-confirmation
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - src/app/app.routes.ts
  - src/app/core/models/booking.models.ts
  - src/app/core/services/booking.service.ts
  - src/app/pages/book-placeholder/book-placeholder.page.ts
  - src/app/pages/book-placeholder/book-placeholder.page.html
  - src/app/pages/book-placeholder/book-placeholder.page.scss
  - src/app/pages/payment-return/payment-return.page.ts
  - src/app/pages/payment-return/payment-return.page.html
  - src/app/pages/payment-return/payment-return.page.scss
  - src/app/pages/booking-success/booking-success.page.ts
  - src/app/pages/booking-success/booking-success.page.html
  - src/app/pages/booking-success/booking-success.page.scss
autonomous: true
requirements: [PAY-02]
user_setup: []

must_haves:
  truths:
    - "Awaiting-payment booking screen shows `Pay now` and opens Stripe Checkout via redirect when tapped (D-03)."
    - "On success return, the app shows `Payment received` + `Confirming your booking…`, polls booking status briefly, and routes to success screen when confirmed (D-12)."
    - "On cancel return, the app routes back to the booking and shows `Payment cancelled` + `Try again` without changing booking status (D-13, D-08)."
    - "Confirmed booking success screen matches the Phase 4 UI contract and shows key booking details (D-11)."
  artifacts:
    - path: "src/app/pages/payment-return/payment-return.page.ts"
      provides: "Dedicated return handler for Stripe success/cancel outcomes"
    - path: "src/app/pages/booking-success/booking-success.page.html"
      provides: "Success screen aligned to `src/design/booking_success/code.html`"
    - path: "src/app/pages/book-placeholder/book-placeholder.page.html"
      provides: "Pay now CTA on awaiting-payment booking created state"
  key_links:
    - from: "src/app/pages/book-placeholder/book-placeholder.page.ts"
      to: "backend checkout-session endpoint"
      via: "BookingService method that returns checkoutUrl"
      pattern: "checkout"
    - from: "src/app/pages/payment-return/payment-return.page.ts"
      to: "GET booking status"
      via: "polling"
      pattern: "setTimeout|interval|poll"
---

<objective>
Implement Ionic/Angular payment entry + return UX per UI-SPEC: “Pay now” on the awaiting-payment booking screen, a return handler that polls confirmation status, and a dedicated booking success screen.

Purpose: Deliver user-facing payment completion clarity while keeping the webhook as the only confirmation source of truth (D-07/D-12).
Output: New routes/screens and BookingService methods that drive the end-to-end UX for PAY-02.
</objective>

<execution_context>
@D:/programming/champ-app/.cursor/get-shit-done/workflows/execute-plan.md
@D:/programming/champ-app/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-payments-confirmation/04-CONTEXT.md
@.planning/phases/04-payments-confirmation/04-UI-SPEC.md

@src/app/app.routes.ts
@src/app/pages/book-placeholder/book-placeholder.page.ts
@src/app/pages/book-placeholder/book-placeholder.page.html
@src/app/core/services/booking.service.ts
@src/app/core/models/booking.models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client API methods + models for checkout + booking polling</name>
  <read_first>
    - `src/app/core/services/booking.service.ts` (withCredentials pattern)
    - `src/app/core/models/booking.models.ts` (Booking shape currently used)
    - D-14 in `04-CONTEXT.md` (cookie-based auth)
  </read_first>
  <files>
    src/app/core/models/booking.models.ts
    src/app/core/services/booking.service.ts
  </files>
  <action>
    Extend the frontend booking API layer to support Phase 4:
    - Add `getBooking(bookingId: string)` calling `GET {apiUrl}/bookings/:id` with `withCredentials: true`.
    - Add `createCheckoutSession(bookingId: string)` calling the backend create/reuse session endpoint (from plan 04-02) and returning `{ checkoutUrl }`.
    Update models:
    - Expand `Booking` to include any new fields needed for UX rendering (at minimum keep `id`, `status`, `expiresAtUtc`, and slot ids).
    - Keep types strict; do not use `any`.
  </action>
  <acceptance_criteria>
    - Both API calls include `withCredentials: true` (D-14)
    - Methods are used by UI tasks (no dead code)
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Frontend can start checkout and poll booking status using typed, cookie-authenticated API calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add “Pay now” CTA on awaiting-payment booking screen (per UI-SPEC Screen D)</name>
  <read_first>
    - `src/app/pages/book-placeholder/book-placeholder.page.html` (created state markup)
    - `.planning/phases/04-payments-confirmation/04-UI-SPEC.md` Screen D contract + copywriting
    - Decisions D-03/D-04/D-06 in `04-CONTEXT.md`
  </read_first>
  <files>
    src/app/pages/book-placeholder/book-placeholder.page.ts
    src/app/pages/book-placeholder/book-placeholder.page.html
    src/app/pages/book-placeholder/book-placeholder.page.scss
  </files>
  <action>
    Extend the existing `step === 'created'` (awaiting-payment) UI:
    - Add primary gradient CTA labeled **Pay now** (or loading label “Opening checkout…” while disabled) that calls the backend to create/reuse a Stripe Checkout session (D-03/D-04) and then redirects the browser to the returned `checkoutUrl`.
    - Disable double-taps during loading; show inline tonal error state matching UI-SPEC copy if session creation fails.
    - Keep existing “Change time” secondary action; add “Try again” label on retry after an error.
    Redirect behavior should follow the app’s existing patterns (plain `window.location.href = checkoutUrl` is acceptable for hosted Checkout redirect).
  </action>
  <acceptance_criteria>
    - Tapping Pay now triggers a single API call and then redirects to Stripe Checkout URL
    - Errors display the UI-SPEC microcopy and allow retry without creating multiple active sessions (D-04 enforced server-side, but UI must not spam)
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Awaiting-payment screen includes the payment entry affordance and handles failure/retry cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add payment return handler + booking success screen (per D-11..D-13)</name>
  <read_first>
    - `.planning/phases/04-payments-confirmation/04-UI-SPEC.md` Screen E + F contracts + locked copy
    - `src/app/app.routes.ts` (route conventions)
    - Decision D-12 (polling behavior) and D-13 (cancel routing)
  </read_first>
  <files>
    src/app/app.routes.ts
    src/app/pages/payment-return/payment-return.page.ts
    src/app/pages/payment-return/payment-return.page.html
    src/app/pages/payment-return/payment-return.page.scss
    src/app/pages/booking-success/booking-success.page.ts
    src/app/pages/booking-success/booking-success.page.html
    src/app/pages/booking-success/booking-success.page.scss
  </files>
  <action>
    Add 2 new routes:
    - `/pay/return` (or similar) → `PaymentReturnPage`
    - `/booking/success` → `BookingSuccessPage`
    Payment return behavior (per D-12/D-13 and UI-SPEC):
    - Parse query params: `bookingId` plus a result flag (success/cancel) matching what backend sets in `success_url` and `cancel_url`.
    - If cancel: navigate back to `/book?...` created state (use existing booking route context if available; otherwise route to booking screen with `bookingId` loaded) and show “Payment cancelled” feedback + **Try again** affordance.
    - If success:
      - Immediately fetch booking by id; if already `confirmed`, route to success screen.
      - If not confirmed, show **Payment received** + **Confirming your booking…** and poll `GET /bookings/:id` with 1–2s backoff, stopping within ~10–15s (UI-SPEC).
      - If confirmed during polling, route to success screen.
      - If still not confirmed after polling, show the calm guidance line and provide escape CTA: **Go to My bookings** (use `/profile` as placeholder navigation target for now if Phase 5 route doesn’t exist yet, but do not invent new Phase 5 screens).
    Success screen:
    - Implement layout aligned to `src/design/booking_success/code.html` using Ionic primitives and UI-SPEC tokens/copy (headline “Booking confirmed”, primary CTA “Go to My bookings”, optional “Add to calendar” button).
  </action>
  <acceptance_criteria>
    - Return screen never marks bookings confirmed locally; it only displays + polls (D-12)
    - Cancel return keeps booking `awaiting_payment` and offers retry (D-08/D-13)
    - Success screen matches UI-SPEC copy + tonal layering/no-line rule constraints
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Payment return and success UX are implemented per contract and wired via routes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Stripe → client return URL | Query params are untrusted; return is not proof of payment (D-12). |
| client → API | Polling and session creation must use cookie auth (D-14). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | return URL params | mitigate | Treat return outcome as UI hint only; always fetch booking status from API; never confirm based on query params (D-12). |
| T-04-14 | Information Disclosure | UI/logging | mitigate | Do not log full booking objects or secrets to console; render only necessary booking details. |
</threat_model>

<verification>
- `npm test` (frontend)
</verification>

<success_criteria>
- Users are returned with a clear success/cancel UX, with a race-safe “confirming…” polling state that leads to a dedicated success screen once webhook confirmation lands.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payments-confirmation/04-04-SUMMARY.md`
</output>

