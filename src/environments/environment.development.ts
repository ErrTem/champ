export const environment = {
  production: false,
  /** Dev server proxy: see `proxy.conf.json` → Nest (docker) on :3001 */
  apiUrl: '/api',
  /** Use backend APIs in dev by default. */
  mockCatalog: false,
};
